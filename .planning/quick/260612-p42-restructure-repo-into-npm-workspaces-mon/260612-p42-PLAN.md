---
phase: quick-260612-p42
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - packages/core/src/registry-types.ts
  - packages/core/src/loadRegistry.ts
  - packages/core/src/parseSpecDoc.ts
  - packages/core/src/scoring.ts
  - tests/spec/helpers.loadRegistry.test.ts
  - tests/spec/helpers.parseSpecDoc.test.ts
  - tests/spec/helpers.scoring.test.ts
  - tests/spec/registry.rules.test.ts
  - tests/spec/registry.schema.test.ts
  - tests/spec/doc-registry-sync.test.ts
  - tests/spec/scoring-example.test.ts
  - package-lock.json
autonomous: true
requirements: [INFRA-WORKSPACE-01]

must_haves:
  truths:
    - "npm workspaces root resolves @voke/core as a workspace package (installed/symlinked into node_modules)"
    - "spec helper logic (loadRegistry, parseSpecDoc, scoring, registry types) lives in packages/core/src and is exported from a barrel"
    - "All tests/spec/* import the helpers via @voke/core, not via ../../spec/* relative paths"
    - "npx vitest run passes exactly 31 tests across 8 files"
    - "npx tsc --noEmit (or tsc --build) is clean across the workspace; root references packages/core"
    - "spec/mtqs-v0.1.yaml, spec/MTQS-v0.1.md, spec/SCOPE.md remain in root spec/ and are NOT bundled into packages/core"
    - "prd.md is tracked by git at repo root"
  artifacts:
    - path: "packages/core/package.json"
      provides: "@voke/core package manifest (ESM, NodeNext, exports map, zod+js-yaml runtime deps)"
      contains: "@voke/core"
    - path: "packages/core/src/index.ts"
      provides: "Public API barrel for @voke/core"
      min_lines: 3
    - path: "packages/core/src/loadRegistry.ts"
      provides: "loadRegistry + loadRegistryFile (moved from spec/helpers)"
      exports: ["loadRegistry", "loadRegistryFile"]
    - path: "packages/core/src/scoring.ts"
      provides: "scoring helpers (moved from spec/helpers)"
      exports: ["penaltyFor", "scoreTool", "tierFor", "applyCaps", "serverScore"]
    - path: "packages/core/tsconfig.json"
      provides: "Per-package tsconfig (NodeNext, composite for project references)"
      contains: "NodeNext"
    - path: "package.json"
      provides: "Workspaces root manifest"
      contains: "workspaces"
  key_links:
    - from: "tests/spec/*.test.ts"
      to: "@voke/core"
      via: "ESM import resolved through npm workspace symlink + package exports map"
      pattern: "from '@voke/core'"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/{loadRegistry,parseSpecDoc,scoring,registry-types}.ts"
      via: "barrel re-export"
      pattern: "export .* from './"
    - from: "tsconfig.json (root)"
      to: "packages/core/tsconfig.json"
      via: "project reference"
      pattern: "references"
---

<objective>
Restructure the voke repo into an npm workspaces monorepo with a single package, `@voke/core`, that holds the spec helper logic. The published-standard files (registry YAML + spec markdown) stay in the root `spec/` directory; only the code moves.

Purpose: Establish the workspace layout so Phases 4 (CLI) and 5 (Action) have clean package boundaries to slot into, without touching the spec content or breaking the 31 passing spec tests.

Output: `packages/core` as `@voke/core` (ESM/NodeNext, exports barrel); root converted to a workspaces root with shared dev tooling; all `tests/spec/*` repointed to `@voke/core`; project-reference tsconfig that keeps `tsc` clean; `prd.md` tracked.

Scope guard (locked by user): DO NOT create `packages/cli` or `packages/action`. Leave `packages/` ready for them only. Keep everything local — no git remote, no push.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md

<!-- Current root config being converted -->
@package.json
@tsconfig.json
@vitest.config.ts

<!-- Source files moving into @voke/core (move verbatim; only fix relative .js import specifiers) -->
@spec/registry-types.ts
@spec/helpers/loadRegistry.ts
@spec/helpers/parseSpecDoc.ts
@spec/helpers/scoring.ts

<interfaces>
<!-- Public API the test suite consumes. The @voke/core barrel MUST export ALL of these. -->
<!-- Extracted from tests/spec/*.test.ts import statements — do NOT explore, use this list. -->

Value exports (functions):
  - loadRegistry        (from loadRegistry.ts)
  - loadRegistryFile    (from loadRegistry.ts)
  - parseSpecDoc        (from parseSpecDoc.ts)
  - penaltyFor          (from scoring.ts)
  - scoreTool           (from scoring.ts)
  - tierFor             (from scoring.ts)
  - applyCaps           (from scoring.ts)
  - serverScore         (from scoring.ts)

Type-only exports:
  - RuleRegistryEntry   (from registry-types.ts)  // used: import type in registry.rules.test.ts
  - Finding             (from scoring.ts)          // used: import type in scoring + scoring-example tests
  - Also re-export for public API completeness: Severity, DimensionId, RuleScope,
    RuleRegistryEntry, ParsedSpecDoc, SpecDocSection
  - Also re-export the zod schemas already exported by registry-types.ts:
    SeveritySchema, DimensionSchema, ScopeSchema, RuleRegistryEntrySchema, RuleRegistrySchema

Test files and the exact import line each currently uses (ALL helper imports must change to '@voke/core'):
  - helpers.loadRegistry.test.ts : import { loadRegistry } from '../../spec/helpers/loadRegistry.js'
  - helpers.parseSpecDoc.test.ts : import { parseSpecDoc } from '../../spec/helpers/parseSpecDoc.js'
  - helpers.scoring.test.ts      : import { penaltyFor, scoreTool, tierFor, applyCaps, serverScore } from '../../spec/helpers/scoring.js'
                                   + import type { Finding } from '../../spec/helpers/scoring.js'
  - registry.rules.test.ts       : import { loadRegistryFile } from '../../spec/helpers/loadRegistry.js'
                                   + import type { RuleRegistryEntry } from '../../spec/registry-types.js'
  - registry.schema.test.ts      : import { loadRegistryFile } from '../../spec/helpers/loadRegistry.js'
  - doc-registry-sync.test.ts    : import { parseSpecDoc } from '../../spec/helpers/parseSpecDoc.js'
                                   + import { loadRegistryFile } from '../../spec/helpers/loadRegistry.js'
  - scoring-example.test.ts      : import { scoreTool, tierFor, applyCaps, serverScore } from '../../spec/helpers/scoring.js'
                                   + import type { Finding } from '../../spec/helpers/scoring.js'
  - scope.test.ts                : NO helper import — reads spec/SCOPE.md via process.cwd(); DO NOT modify.

CRITICAL — path-based reads stay anchored to repo root spec/ (do NOT touch these lines):
  - doc-registry-sync.test.ts uses: resolve(import.meta.dirname, '..', '..') then 'spec/MTQS-v0.1.md' / 'spec/mtqs-v0.1.yaml'
  - registry.rules.test.ts + registry.schema.test.ts use: resolve(process.cwd(), 'spec/mtqs-v0.1.yaml')
  - scope.test.ts uses: join(process.cwd(), 'spec', 'SCOPE.md')
  Vitest runs from repo root, so these resolve to root spec/ unchanged. This is the reason the YAML/markdown MUST stay in root spec/ and MUST NOT be copied into packages/core. loadRegistryFile takes an explicit path arg — core has no default path to bundle, so the constraint is satisfied as long as no spec data files are copied.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @voke/core package and move helper code into it</name>
  <files>packages/core/package.json, packages/core/tsconfig.json, packages/core/src/registry-types.ts, packages/core/src/loadRegistry.ts, packages/core/src/parseSpecDoc.ts, packages/core/src/scoring.ts, packages/core/src/index.ts</files>
  <action>
    Create the @voke/core package. Use `git mv` to preserve history when moving existing files.

    1. Move the four source files into packages/core/src/ (flatten the helpers/ subdir):
       - git mv spec/registry-types.ts packages/core/src/registry-types.ts
       - git mv spec/helpers/loadRegistry.ts packages/core/src/loadRegistry.ts
       - git mv spec/helpers/parseSpecDoc.ts packages/core/src/parseSpecDoc.ts
       - git mv spec/helpers/scoring.ts packages/core/src/scoring.ts
       Then remove the now-empty helpers dir: rmdir spec/helpers (must be empty — fail loudly if not).
       DO NOT move spec/mtqs-v0.1.yaml, spec/MTQS-v0.1.md, or spec/SCOPE.md — they STAY in root spec/ (published standard, per user lock).

    2. Fix internal relative import specifiers after the move. The helpers previously imported from '../registry-types.js'; now registry-types.ts is a sibling:
       - loadRegistry.ts: change `from '../registry-types.js'` to `from './registry-types.js'` (both the value import and the `import type` line).
       - scoring.ts: change `from '../registry-types.js'` to `from './registry-types.js'`.
       - parseSpecDoc.ts has no cross-file imports — leave its body unchanged.
       Keep the `.js` extension on all relative specifiers (required for NodeNext ESM). Do NOT otherwise edit the helper logic — these moves are verbatim.

    3. Create packages/core/src/index.ts as the public API barrel exporting the full surface from <interfaces>:
         export { loadRegistry, loadRegistryFile } from './loadRegistry.js';
         export { parseSpecDoc } from './parseSpecDoc.js';
         export type { ParsedSpecDoc, SpecDocSection } from './parseSpecDoc.js';
         export { penaltyFor, scoreTool, tierFor, applyCaps, serverScore } from './scoring.js';
         export type { Finding } from './scoring.js';
         export { SeveritySchema, DimensionSchema, ScopeSchema, RuleRegistryEntrySchema, RuleRegistrySchema } from './registry-types.js';
         export type { Severity, DimensionId, RuleScope, RuleRegistryEntry } from './registry-types.js';
       (scoring.ts also re-exports Severity/DimensionId as types — fine; registry-types.ts is the canonical source in the barrel, so do NOT also re-export those two from scoring to avoid a duplicate-export conflict.)

    4. Create packages/core/package.json — ESM, NodeNext, exports map pointing at the TS source (vitest/esbuild and `tsc` resolve TS directly, so no compiled dist is needed for this restructure to pass). zod + js-yaml become runtime deps of the package that uses them; @types/js-yaml becomes a devDep of this package:
         {
           "name": "@voke/core",
           "version": "0.0.0",
           "description": "Voke MTQS core: deterministic registry loading, spec-doc parsing, and scoring helpers",
           "type": "module",
           "private": true,
           "engines": { "node": ">=22" },
           "exports": { ".": { "types": "./src/index.ts", "import": "./src/index.ts" } },
           "scripts": { "build": "tsup src/index.ts --format esm --dts --clean", "typecheck": "tsc --noEmit" },
           "dependencies": { "js-yaml": "^4.1.0", "zod": "4.4.3" },
           "devDependencies": { "@types/js-yaml": "^4.0.9" }
         }
       The tsup build script is declared for when CLI/Action consume a built artifact later; it is NOT required to pass this plan.

    5. Create packages/core/tsconfig.json — NodeNext, composite (so the root can reference it), strict, no `any`:
         {
           "compilerOptions": {
             "target": "ES2022",
             "module": "NodeNext",
             "moduleResolution": "NodeNext",
             "strict": true,
             "composite": true,
             "declaration": true,
             "outDir": "dist",
             "rootDir": "src",
             "skipLibCheck": true,
             "types": ["node"]
           },
           "include": ["src/**/*.ts"]
         }
  </action>
  <verify>
    <automated>test -f packages/core/src/index.ts && test -f packages/core/src/scoring.ts && test -f packages/core/package.json && test -f packages/core/tsconfig.json && test ! -e spec/helpers && test -f spec/mtqs-v0.1.yaml && test -f spec/MTQS-v0.1.md && test -f spec/SCOPE.md && grep -q '@voke/core' packages/core/package.json && grep -q "from './registry-types.js'" packages/core/src/loadRegistry.ts && echo OK</automated>
  </verify>
  <done>@voke/core exists with all four helpers moved into src/, internal imports fixed to sibling `./registry-types.js`, a barrel exporting the full public API, an ESM/NodeNext package.json (zod + js-yaml as deps, @types/js-yaml as devDep), and a composite tsconfig. The three spec data/markdown files remain in root spec/ and spec/helpers/ no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Convert root to a workspaces root, repoint configs and test imports, install</name>
  <files>package.json, tsconfig.json, vitest.config.ts, tests/spec/helpers.loadRegistry.test.ts, tests/spec/helpers.parseSpecDoc.test.ts, tests/spec/helpers.scoring.test.ts, tests/spec/registry.rules.test.ts, tests/spec/registry.schema.test.ts, tests/spec/doc-registry-sync.test.ts, tests/spec/scoring-example.test.ts, package-lock.json</files>
  <action>
    1. Convert root package.json into a workspaces root:
       - Add "workspaces": ["packages/*"].
       - Keep shared DEV tooling at the root: typescript, vitest, @types/node. ADD tsup (^8.5.1 per Tech Stack) to root devDependencies so future package builds work.
       - REMOVE the package-specific deps that moved to @voke/core: delete js-yaml, @types/js-yaml, and zod from ROOT devDependencies (they now live in packages/core/package.json).
       - Update root scripts: keep "test": "vitest run"; change "typecheck" to "tsc --build" (uses project references to keep the whole workspace clean). For "test:spec", change to "vitest run tests/spec/" (the old "vitest run spec/" path no longer holds tests).
       - Resulting root devDependencies should be roughly: @types/node, tsup, typescript, vitest.

    2. Update root tsconfig.json to use project references and stop directly compiling the moved spec helpers:
         {
           "compilerOptions": {
             "target": "ES2022",
             "module": "NodeNext",
             "moduleResolution": "NodeNext",
             "strict": true,
             "noEmit": true,
             "composite": true,
             "skipLibCheck": true,
             "types": ["node"]
           },
           "references": [{ "path": "./packages/core" }],
           "include": ["tests/**/*.ts"]
         }
       Switch root from Bundler to NodeNext resolution so @voke/core's exports map resolves identically under tsc and vitest. `include` now only covers tests/ (helpers are owned by the packages/core project).

    3. Repoint EVERY helper/registry import in tests/spec/* from the old relative `../../spec/...` paths to `@voke/core`, using the exact mapping in <interfaces>. Replace the source specifier with '@voke/core' and merge value + type imports where listed. Per-file target import lines:
       - helpers.loadRegistry.test.ts: import { loadRegistry } from '@voke/core';
       - helpers.parseSpecDoc.test.ts: import { parseSpecDoc } from '@voke/core';
       - helpers.scoring.test.ts: import { penaltyFor, scoreTool, tierFor, applyCaps, serverScore } from '@voke/core';  +  import type { Finding } from '@voke/core';
       - registry.rules.test.ts: import { loadRegistryFile } from '@voke/core';  +  import type { RuleRegistryEntry } from '@voke/core';
       - registry.schema.test.ts: import { loadRegistryFile } from '@voke/core';
       - doc-registry-sync.test.ts: import { parseSpecDoc } from '@voke/core';  +  import { loadRegistryFile } from '@voke/core';
       - scoring-example.test.ts: import { scoreTool, tierFor, applyCaps, serverScore } from '@voke/core';  +  import type { Finding } from '@voke/core';
       DO NOT change the path-based readFileSync/resolve/join lines that point at root spec/ files (they stay anchored to repo root — see <interfaces> CRITICAL note). DO NOT modify scope.test.ts.

    4. Keep vitest.config.ts globbing tests at the repo root (include ['tests/**/*.test.ts'] — unchanged). @voke/core resolves through the npm-workspace symlink in node_modules. If vitest cannot resolve @voke/core after install, add a resolve.alias mapping '@voke/core' -> path.resolve(__dirname, 'packages/core/src/index.ts') as a fallback — but try the clean workspace-install path first.

    5. Run `npm install` from the repo root to create the workspace symlink (node_modules/@voke/core -> packages/core) and regenerate package-lock.json. Track prd.md with `git add prd.md` (currently untracked; stays at repo root and gets committed). Keep everything local — no git remote, no push.
  </action>
  <verify>
    <automated>npm install >/dev/null 2>&1 && test -e node_modules/@voke/core && grep -q '"workspaces"' package.json && ! grep -rq "spec/helpers" tests/spec/ && grep -q "@voke/core" tests/spec/helpers.scoring.test.ts && npx tsc --build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -12</automated>
  </verify>
  <done>Root is an npm workspaces root (workspaces: ["packages/*"]) with shared dev tooling only (typescript, vitest, @types/node, tsup); zod/js-yaml/@types/js-yaml removed from root. node_modules/@voke/core symlink exists. Root tsconfig references packages/core. All tests/spec/* import helpers from @voke/core (no remaining `spec/helpers` references). prd.md is tracked. WHOLE-TASK GATE: `npx vitest run` reports 31 tests passing across 8 files and `npx tsc --build` is clean.</done>
</task>

</tasks>

<verification>
Whole-plan gate (must all hold):
- `npx vitest run` → exactly 31 tests passing across 8 test files (unchanged from before the restructure).
- `npx tsc --build` (project references) → clean, zero errors across root + packages/core.
- `node_modules/@voke/core` symlink resolves to packages/core.
- `grep -rn "spec/helpers" tests/` returns nothing (all imports repointed).
- spec/mtqs-v0.1.yaml, spec/MTQS-v0.1.md, spec/SCOPE.md still present in root spec/; spec/helpers/ removed; no spec data files copied into packages/core.
- `git status` shows prd.md tracked; no packages/cli or packages/action created.
</verification>

<success_criteria>
- Repo is an npm workspaces monorepo with a single package @voke/core holding all spec helper logic and a public barrel.
- All tests/spec/* consume @voke/core; the 31-test suite passes unchanged.
- `tsc --build` clean across the workspace via project references.
- Published-standard files remain in root spec/ and are not bundled into the package.
- prd.md tracked; packages/ left ready (empty of cli/action) for Phases 4/5.
</success_criteria>

<output>
After completion, create `.planning/quick/260612-p42-restructure-repo-into-npm-workspaces-mon/260612-p42-SUMMARY.md`
</output>
