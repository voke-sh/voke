---
phase: 02-engine-ingestion-determinism
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - packages/linter/src/report/types.ts
  - packages/linter/src/report/builder.ts
  - packages/linter/src/index.ts
  - tests/report/builder.test.ts
  - tests/engine/determinism.test.ts
autonomous: true
requirements: [ENG-04]
user_setup: []

must_haves:
  truths:
    - "A LintReport is built from a VokeSnapshot + engine findings using @voke/core scoring (no scoring reimplemented)"
    - "The report serializes with sorted keys and excludes the meta/capturedAt provenance block from the hashed/compared body"
    - "Running the engine 3 consecutive times on the committed Apideck fixture produces byte-identical serialized output"
    - "Shuffling the input tool order produces identical output (sort-on-run enforced)"
    - "snapshotContentHash is the SHA-256 of the canonical sorted tools surface"
  artifacts:
    - path: "packages/linter/src/report/builder.ts"
      provides: "buildReport: VokeSnapshot + Finding[] -> LintReport using @voke/core scoring"
      exports: ["buildReport", "serializeReportBody"]
    - path: "tests/engine/determinism.test.ts"
      provides: "the ENG-04 / D-12 byte-identical x3 proof artifact"
      contains: "byte-identical"
  key_links:
    - from: "packages/linter/src/report/builder.ts"
      to: "@voke/core"
      via: "scoreTool/applyCaps/tierFor/serverScore (reused, not reimplemented)"
      pattern: "scoreTool"
    - from: "tests/engine/determinism.test.ts"
      to: "packages/linter/src/report/builder.ts"
      via: "serializeReportBody called 3x and compared"
      pattern: "serializeReportBody"
---

<objective>
Tie ingestion + engine + scoring + canonicalization together into the phase's executable definition of done: a report builder that assembles a LintReport from a VokeSnapshot and engine findings (reusing @voke/core scoring), a canonical body-serializer that strips the meta/capturedAt block, and the byte-identical x3 determinism test running on the committed Apideck fixture. This is the proof artifact that the whole determinism wedge holds.

Purpose: ENG-04 / D-12. Everything in Plans 01-03 exists to make this test pass byte-identically three times. Wiring it now closes the phase loop and gives CI the artifact it consumes.
Output: report types + builder, and the determinism test (x3 byte-identical + shuffled-input invariance).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-engine-ingestion-determinism/02-CONTEXT.md
@.planning/phases/02-engine-ingestion-determinism/02-RESEARCH.md
@.planning/research/ARCHITECTURE.md

<interfaces>
From @voke/core (REUSE — do not reimplement scoring; CONTEXT.md is explicit):
  import { scoreTool, applyCaps, tierFor, serverScore } from '@voke/core';
  import type { Finding as CoreFinding, DimensionId } from '@voke/core';  // CoreFinding = { ruleId, severity, dimension }
  scoreTool(findings: CoreFinding[]): number;          // 100 - sum(penaltyFor), integer-first
  applyCaps(rawScore: number, ruleIds: string[]): number;  // hard tier caps (S01/S03/S06→69, S04/A06/D03→79)
  tierFor(score: number): 'A'|'B'|'C'|'D'|'F';
  serverScore(toolScores: number[]): number;           // Math.round(mean)

From Plan 03 engine/types.ts: Finding (runtime) = { ruleId, dimension, severity, message, location:{tool,path}, fixHint }. To feed @voke/core scoring, map each runtime Finding to a CoreFinding = { ruleId, severity, dimension } (drop location/message/fixHint for scoring).

From Plan 02 ingestion/types.ts: VokeSnapshot { snapshotVersion, mtqsVersion, server, meta:{capturedAt}, tools: ToolSnapshot[] }.

From Plan 01 canonicalize: canonicalJson, surfaceContentHash.

From Plan 03 engine: runRules(surface, registry, config); createDefaultRegistry() (empty sealed in Phase 2 → zero findings, scores all 100 — still a valid byte-identical artifact).

LintReport shape (ARCHITECTURE.md lines 271-298, adjusted for D-01/D-02 — note generatedAt moves into a meta block excluded from the compared body):
  interface LintReport {
    vokeVersion: string;
    mtqsVersion: string;
    meta: { generatedAt: string };            // D-02: provenance, EXCLUDED from serializeReportBody
    server: ServerIdentity;
    snapshotContentHash: string;              // surfaceContentHash(tools)
    tools: Array<{ toolId: string; contentHash: string; findings: Finding[]; score: number; tier: Tier }>;
    serverScore: number;
    serverTier: Tier;
  }

D-12 determinism test: run engine 3x on the saved Apideck fixture, serialize the LintReport body with sorted keys, STRIP meta (generatedAt/capturedAt), assert the 3 strings are byte-identical. The body excludes meta entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LintReport types + buildReport (reusing @voke/core scoring) + serializeReportBody</name>
  <files>packages/linter/src/report/types.ts, packages/linter/src/report/builder.ts, packages/linter/src/index.ts, tests/report/builder.test.ts</files>
  <read_first>
    - /Users/samir.amzani/Projects/voke/.planning/research/ARCHITECTURE.md (LintReport lines 271-298; determinism #3 sort ToolScore by toolId; #6 snapshotContentHash; #7 published weights)
    - /Users/samir.amzani/Projects/voke/.planning/phases/02-engine-ingestion-determinism/02-CONTEXT.md (D-01 LintReport scored artifact; D-02 meta block excluded from hash + determinism test)
    - /Users/samir.amzani/Projects/voke/packages/core/src/scoring.ts (scoreTool/applyCaps/tierFor/serverScore signatures — REUSE)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/engine/types.ts (runtime Finding shape)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/canonicalize/hash.ts (surfaceContentHash) and canonical-json.ts (canonicalJson)
  </read_first>
  <action>
    Create packages/linter/src/report/types.ts:
      import type { ServerIdentity, ToolSnapshot } from '../ingestion/types.js';
      import type { Finding } from '../engine/types.js';
      export type Tier = 'A'|'B'|'C'|'D'|'F';
      export interface ToolReport { toolId: string; contentHash: string; findings: Finding[]; score: number; tier: Tier; }
      export interface LintReport { vokeVersion: string; mtqsVersion: string; meta: { generatedAt: string }; server: ServerIdentity; snapshotContentHash: string; tools: ToolReport[]; serverScore: number; serverTier: Tier; }

    Create packages/linter/src/report/builder.ts:
      - import { scoreTool, applyCaps, tierFor, serverScore } from '@voke/core'; (REUSE — do NOT reimplement any arithmetic)
      - export const buildReport = (snapshot: VokeSnapshot, findings: Finding[], opts?: { vokeVersion?: string }): LintReport => { ... }:
          1. Group findings by location.tool (toolId).
          2. For each tool in snapshot.tools (already sorted by toolId): map its runtime findings to CoreFinding {ruleId,severity,dimension}; raw = scoreTool(coreFindings); capped = applyCaps(raw, ruleIds of that tool's findings); score = capped; tier = tierFor(score). Build ToolReport { toolId, contentHash, findings (the runtime findings for that tool, already engine-sorted), score, tier }.
          3. tools array: keep snapshot.tools order (already toolId-sorted; determinism #1).
          4. serverScore = serverScore(tools.map(t => t.score)) (determinism #3 — input is toolId-sorted); serverTier = tierFor(serverScore).
          5. snapshotContentHash = surfaceContentHash(snapshot.tools) (determinism #6).
          6. meta.generatedAt = new Date().toISOString() (the ONLY wall-clock; D-02 — lives in meta, excluded from body).
          7. vokeVersion = opts?.vokeVersion ?? '0.0.0'; mtqsVersion = snapshot.mtqsVersion.
      - export const serializeReportBody = (report: LintReport): string => { const { meta, ...body } = report; return canonicalJson(body); }; — STRIP meta (D-02/D-12), serialize the rest with the canonical sorted-key serializer. This is the exact string the determinism test compares.

    Add buildReport + serializeReportBody + LintReport/Tier/ToolReport to the barrel (.js extensions).
  </action>
  <verify>
    <automated>npm test -- tests/report/builder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep finds import { scoreTool, applyCaps, tierFor, serverScore } from '@voke/core' in report/builder.ts (scoring reused, not reimplemented)
    - grep confirms report/builder.ts does NOT contain its own penalty/round arithmetic (no Math.round of base*mult; no BASE/MULT redefinition)
    - grep finds export const serializeReportBody and that it destructures `meta` out before canonicalJson (meta excluded, D-02)
    - grep finds surfaceContentHash used for snapshotContentHash
    - grep finds "new Date().toISOString()" only inside the meta assignment (the sole wall-clock value)
    - npm test -- tests/report/builder.test.ts exits 0 (build a report from the fixture + empty findings; assert structure, snapshotContentHash present, serializeReportBody output contains no "generatedAt")
  </acceptance_criteria>
  <done>buildReport assembles a LintReport reusing @voke/core scoring; serializeReportBody emits the canonical, meta-stripped body the determinism test compares.</done>
</task>

<task type="auto">
  <name>Task 2: Byte-identical x3 determinism test (ENG-04 / D-12) + shuffled-input invariance</name>
  <files>tests/engine/determinism.test.ts</files>
  <read_first>
    - /Users/samir.amzani/Projects/voke/.planning/phases/02-engine-ingestion-determinism/02-RESEARCH.md (Pattern 13 determinism test; Validation Architecture → Test Map ENG-04 rows)
    - /Users/samir.amzani/Projects/voke/.planning/phases/02-engine-ingestion-determinism/02-CONTEXT.md (D-12 exact test recipe; D-02 strip meta)
    - /Users/samir.amzani/Projects/voke/tests/fixtures/apideck-snapshot.json (Plan 01 committed fixture — the test input)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/report/builder.ts (buildReport, serializeReportBody — Task 1)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/engine/runner.ts and registry.ts (runRules, createDefaultRegistry — Plan 03)
  </read_first>
  <action>
    Create tests/engine/determinism.test.ts implementing the D-12 recipe (RESEARCH.md Pattern 13):
      - Read tests/fixtures/apideck-snapshot.json (readFileSync + JSON.parse, or readSnapshot from Plan 02) into a VokeSnapshot.
      - Helper runAndSerialize(snapshot): const registry = createDefaultRegistry(); const findings = runRules(snapshot.tools, registry, {}); const report = buildReport(snapshot, findings); return serializeReportBody(report).
      - Test A (byte-identical x3): const r1 = runAndSerialize(snap); const r2 = ...; const r3 = ...; expect(r1).toBe(r2); expect(r2).toBe(r3). This is the ENG-04 proof artifact.
      - Test B (shuffled-input invariance): build a second snapshot with snapshot.tools reversed/shuffled; assert runAndSerialize(shuffled) === runAndSerialize(original) — proves sort-on-run (determinism #1/#5) makes output a function of content, not input order.
      - Test C (meta excluded): assert the serialized body does NOT contain the substring "generatedAt" or "capturedAt" (D-02/D-12).
    NOTE: In Phase 2 the default registry is empty (zero rules) so findings is [] and all tool scores are 100 — that is a VALID byte-identical artifact. The test proves the PIPELINE is deterministic; Phase 3 adds rules and the same test keeps it honest.
  </action>
  <verify>
    <automated>npm test -- tests/engine/determinism.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep finds "apideck-snapshot.json" and serializeReportBody and createDefaultRegistry and runRules in tests/engine/determinism.test.ts
    - the test contains three expect calls proving r1===r2===r3 (byte-identical x3)
    - the test contains a shuffled/reversed-input case asserting equality with the original serialization
    - the test asserts the body excludes "generatedAt" / "capturedAt"
    - npm test -- tests/engine/determinism.test.ts exits 0
    - Full suite green: `npm test` exits 0
  </acceptance_criteria>
  <done>The x3 byte-identical determinism test passes on the committed Apideck fixture, is invariant to input tool order, and excludes the meta provenance block — the phase DoD artifact is green.</done>
</task>

</tasks>

<verification>
- npm test passes for tests/report/ and tests/engine/determinism.test.ts
- Full suite `npm test` is green (canonicalize + ingestion + engine + report)
- `npm run typecheck` succeeds
- serializeReportBody output never contains generatedAt/capturedAt
</verification>

<success_criteria>
- ENG-04 / D-12: engine run 3x on the Apideck fixture → byte-identical serialized body
- Shuffled input order → identical output (sort-on-run proven)
- Scoring is @voke/core reused, not reimplemented
- meta/capturedAt/generatedAt excluded from the hashed/compared body
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-ingestion-determinism/02-04-SUMMARY.md`
</output>
