---
phase: 02-engine-ingestion-determinism
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/linter/package.json
  - packages/linter/tsconfig.json
  - packages/linter/src/index.ts
  - packages/linter/src/canonicalize/canonical-json.ts
  - packages/linter/src/canonicalize/hash.ts
  - tsconfig.json
  - package-lock.json
  - tests/canonicalize/canonical-json.test.ts
  - tests/canonicalize/hash.test.ts
  - tests/fixtures/apideck-snapshot.json
autonomous: true
requirements: [ING-04]
user_setup: []

must_haves:
  truths:
    - "A new packages/linter workspace package exists, builds, and depends on @voke/core + the pinned MCP/ajv stack"
    - "canonicalJson produces byte-identical output for the same object regardless of key insertion order"
    - "canonicalJson never reorders array elements (prefixItems/enum order is semantic)"
    - "sha256 of canonical JSON is stable across runs and locale-independent"
    - "A committed Apideck-shaped snapshot fixture exists for the Wave 2 determinism test"
  artifacts:
    - path: "packages/linter/package.json"
      provides: "linter workspace package with pinned deps"
      contains: "@modelcontextprotocol/sdk"
    - path: "packages/linter/src/canonicalize/canonical-json.ts"
      provides: "deterministic sorted-key JSON serializer"
      exports: ["canonicalJson"]
    - path: "packages/linter/src/canonicalize/hash.ts"
      provides: "SHA-256 helper + tool/surface content hash"
      exports: ["sha256", "toolContentHash", "surfaceContentHash"]
    - path: "tests/fixtures/apideck-snapshot.json"
      provides: "committed VokeSnapshot fixture for determinism test"
      contains: "tools"
  key_links:
    - from: "packages/linter/src/canonicalize/hash.ts"
      to: "packages/linter/src/canonicalize/canonical-json.ts"
      via: "import canonicalJson"
      pattern: "canonicalJson"
    - from: "tsconfig.json"
      to: "packages/linter"
      via: "project reference"
      pattern: "packages/linter"
---

<objective>
Create the new `packages/linter` workspace package (the home for all Phase 2 engine + ingestion code), install the pinned MCP/ajv stack, and build the `canonicalize/` module: the deterministic sorted-key JSON serializer and SHA-256 content-hash helpers that ingestion (ING-04 contentHash) and the Wave 2 determinism test both consume. Also commit an Apideck-shaped `VokeSnapshot` fixture the x3 determinism test will run against.

Purpose: This is the dependency root for the whole phase. Canonicalization is the single source of byte-stable serialization; ingestion and the determinism test cannot exist without it. Doing it first prevents every downstream task from re-deriving the serializer.
Output: A buildable `@voke/linter` package, the canonicalize module with tests, and the committed fixture.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-engine-ingestion-determinism/02-CONTEXT.md
@.planning/phases/02-engine-ingestion-determinism/02-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
Existing root config the new package must align with. Use these directly.

Root tsconfig.json (current): compilerOptions target ES2022, module NodeNext, moduleResolution NodeNext, strict true, noEmit true, composite true, skipLibCheck true, types ["node"]; references [{ path ./packages/core }]; include ["tests/**/*.ts"].

packages/core/tsconfig.json (mirror this): compilerOptions target ES2022, module NodeNext, moduleResolution NodeNext, strict true, composite true, declaration true, outDir "dist", rootDir "src", skipLibCheck true, types ["node"]; include ["src/**/*.ts"].

packages/core/package.json exports shape (mirror for linter): type "module"; exports { ".": { "types": "./src/index.ts", "import": "./src/index.ts" } }.

vitest.config.ts (already globs tests/**/*.test.ts; no change needed): test include ['tests/**/*.test.ts'], environment 'node'.

Canonical JSON serializer (RESEARCH.md Pattern 4 — implement verbatim):
  export const canonicalJson = (value: unknown): string => {
    if (value === null || typeof value !== 'object') return JSON.stringify(value);
    if (Array.isArray(value)) return '[' + value.map(canonicalJson).join(',') + ']';
    const sorted = Object.keys(value as Record<string, unknown>)
      .sort((a, b) => a.localeCompare(b, 'en', { sensitivity: 'variant' }))
      .filter(k => (value as Record<string, unknown>)[k] !== undefined)
      .map(k => JSON.stringify(k) + ':' + canonicalJson((value as Record<string, unknown>)[k]));
    return '{' + sorted.join(',') + '}';
  };
The .filter mirrors JSON.stringify undefined-omission (Pitfall 7). Only object keys are sorted; arrays are never reordered.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/linter workspace package and install pinned deps</name>
  <files>packages/linter/package.json, packages/linter/tsconfig.json, packages/linter/src/index.ts, tsconfig.json, package-lock.json</files>
  <read_first>
    - /Users/samir.amzani/Projects/voke/package.json (root: workspaces glob packages/*, type module, engines node>=22)
    - /Users/samir.amzani/Projects/voke/packages/core/package.json (mirror exports + type module + node engines)
    - /Users/samir.amzani/Projects/voke/packages/core/tsconfig.json (mirror composite tsconfig)
    - /Users/samir.amzani/Projects/voke/tsconfig.json (root: must add the new project reference)
    - /Users/samir.amzani/Projects/voke/CLAUDE.md "Technology Stack" table (pinned versions, npm not pnpm, Node 22)
  </read_first>
  <action>
    Create the new workspace package `packages/linter`:

    1. packages/linter/package.json: name "@voke/linter", version "0.0.0", type "module", private true, engines node ">=22", exports { ".": { "types": "./src/index.ts", "import": "./src/index.ts" } } (mirror @voke/core src-as-entry), scripts { "build": "tsup src/index.ts --format esm --dts --clean", "typecheck": "tsc --noEmit" }. dependencies: "@voke/core": "*", "@modelcontextprotocol/sdk": "~1.29.0", "ajv": "8.20.0", "ajv-formats": "3.0.1", "zod": "4.4.3" (use these EXACT specifiers: ~1.29.0 for the SDK per CLAUDE.md "pin minor"; exact pins for the rest). devDependencies: { "@types/node": "^22.0.0", "tsup": "^8.5.1", "typescript": "^5.6.0" }.

    2. packages/linter/tsconfig.json: copy packages/core/tsconfig.json verbatim (composite, declaration, outDir dist, rootDir src, NodeNext, strict, include src/**/*.ts). Add "references": [{ "path": "../core" }].

    3. packages/linter/src/index.ts: placeholder barrel, write `export {};` for now (updated at end of Task 2).

    4. Root tsconfig.json: add { "path": "./packages/linter" } to the references array (keep the existing ./packages/core entry).

    5. From the repo root with npm workspaces (NOT pnpm): run `npm install @modelcontextprotocol/sdk@~1.29.0 ajv@8.20.0 ajv-formats@3.0.1 zod@4.4.3 --workspace=@voke/linter`, then `npm install` at root to wire workspace symlinks and update package-lock.json.

    Do NOT add any LLM/AI SDK. Do NOT install pnpm. Confirmed versions (npm 2026-06-12): sdk 1.29.0, ajv 8.20.0, ajv-formats 3.0.1.
  </action>
  <verify>
    <automated>npm ls --workspace=@voke/linter @modelcontextprotocol/sdk ajv ajv-formats</automated>
  </verify>
  <acceptance_criteria>
    - packages/linter/package.json exists; grep finds "@modelcontextprotocol/sdk": "~1.29.0"
    - grep finds "ajv": "8.20.0" and "ajv-formats": "3.0.1" in packages/linter/package.json
    - grep finds "@voke/core": "*" in packages/linter/package.json
    - grep finds packages/linter in root tsconfig.json references
    - packages/linter/tsconfig.json contains composite true and a reference to ../core
    - no pnpm-lock.yaml created; package-lock.json updated at repo root
  </acceptance_criteria>
  <done>The @voke/linter workspace package exists with the exact pinned stack installed, typechecks against @voke/core, and is referenced from the root tsconfig.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonicalize module (canonical-json + hash) with tests</name>
  <files>packages/linter/src/canonicalize/canonical-json.ts, packages/linter/src/canonicalize/hash.ts, packages/linter/src/index.ts, tests/canonicalize/canonical-json.test.ts, tests/canonicalize/hash.test.ts</files>
  <read_first>
    - /Users/samir.amzani/Projects/voke/.planning/research/PITFALLS.md (Pitfall 1: Object.keys order, float accumulation, locale-aware sort; lines 16-31)
    - /Users/samir.amzani/Projects/voke/.planning/phases/02-engine-ingestion-determinism/02-RESEARCH.md (Pattern 4 canonicalJson, Pattern 5 hash helpers, Pitfall 7 undefined omission)
    - /Users/samir.amzani/Projects/voke/tests/spec/helpers.scoring.test.ts (vitest conventions: describe/it/expect, import style)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/index.ts (the barrel to update)
  </read_first>
  <behavior>
    - canonicalJson: same object, keys inserted in two different orders, identical string output
    - canonicalJson: arrays preserve element order (["b","a"] stays ["b","a"], NOT sorted)
    - canonicalJson: nested objects recursively key-sorted
    - canonicalJson: undefined-valued keys omitted (matches JSON.stringify)
    - canonicalJson: a string $ref value like "#/$defs/X" preserved byte-for-byte (D-07)
    - canonicalJson: locale-independent — diacritic keys sort the same under LC_ALL=C and en_US
    - sha256: deterministic 64-char hex digest; same input, same hex
    - toolContentHash: hashes canonical JSON of exactly {name, description, inputSchema, outputSchema, annotations} (D-03); reordered keys, same hash
    - surfaceContentHash: sorts tools by toolId via localeCompare('en',{sensitivity:'variant'}) before hashing; input order does not change the hash
  </behavior>
  <action>
    Create packages/linter/src/canonicalize/canonical-json.ts exporting canonicalJson(value: unknown): string EXACTLY as in the interfaces block (recursive; sorts only object keys with localeCompare('en', { sensitivity: 'variant' }); never sorts arrays; omits undefined-valued keys via .filter).

    Create packages/linter/src/canonicalize/hash.ts:
      import { createHash } from 'node:crypto';
      import { canonicalJson } from './canonical-json.js';
      export const sha256 = (input: string): string => createHash('sha256').update(input, 'utf8').digest('hex');
      export const toolContentHash = (tool: { name: string; description?: string; inputSchema: unknown; outputSchema?: unknown; annotations?: unknown; }): string =>
        sha256(canonicalJson({ name: tool.name, description: tool.description, inputSchema: tool.inputSchema, outputSchema: tool.outputSchema, annotations: tool.annotations }));
      export const surfaceContentHash = (tools: ReadonlyArray<{ toolId: string }>): string => {
        const sorted = [...tools].sort((a, b) => a.toolId.localeCompare(b.toolId, 'en', { sensitivity: 'variant' }));
        return sha256(canonicalJson(sorted));
      };
    Import paths MUST use the .js extension (NodeNext ESM, as @voke/core does).

    Update packages/linter/src/index.ts to barrel-export both modules:
      export { canonicalJson } from './canonicalize/canonical-json.js';
      export { sha256, toolContentHash, surfaceContentHash } from './canonicalize/hash.js';

    Write tests RED-first per TDD: tests/canonicalize/canonical-json.test.ts and tests/canonicalize/hash.test.ts, importing from the workspace name @voke/linter (mirror how tests/spec imports @voke/core). Cover every <behavior> bullet. For locale independence, assert canonicalJson on an object with diacritic keys equals a committed expected string constant so a locale shift in CI fails the assertion.
  </action>
  <verify>
    <automated>npm test -- tests/canonicalize/</automated>
  </verify>
  <acceptance_criteria>
    - grep finds export const canonicalJson in packages/linter/src/canonicalize/canonical-json.ts
    - grep finds localeCompare('en', { sensitivity: 'variant' }) in canonical-json.ts
    - grep finds createHash('sha256') in packages/linter/src/canonicalize/hash.ts
    - grep finds export const toolContentHash and export const surfaceContentHash in hash.ts
    - grep finds canonicalize/canonical-json.js in packages/linter/src/index.ts (barrel wired with .js extension)
    - npm test -- tests/canonicalize/ exits 0 with 9+ passing assertions covering: key-order independence, array-order preservation, nested sort, undefined omission, $ref preservation, locale independence, sha256 determinism, toolContentHash 5-field selection, surfaceContentHash input-order independence
  </acceptance_criteria>
  <done>canonicalJson + hash helpers implemented, barrel-exported, and all canonicalize tests pass — byte-stability and locale independence proven by test.</done>
</task>

<task type="auto">
  <name>Task 3: Create and commit the Apideck snapshot fixture</name>
  <files>tests/fixtures/apideck-snapshot.json, tests/fixtures/validate-snapshot.mjs</files>
  <read_first>
    - /Users/samir.amzani/Projects/voke/.planning/research/ARCHITECTURE.md (VokeSnapshot + ToolSnapshot shapes, lines 127-164)
    - /Users/samir.amzani/Projects/voke/.planning/phases/02-engine-ingestion-determinism/02-CONTEXT.md (D-01 raw surface no scores; D-02 capturedAt in meta; D-03 toolId/contentHash)
    - /Users/samir.amzani/Projects/voke/spec/MTQS-v0.1.md (lines 1100-1160: search + crm_search_contacts worked examples — reuse these tool shapes)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/canonicalize/hash.ts (use toolContentHash to compute committed contentHash values)
  </read_first>
  <action>
    Create tests/fixtures/apideck-snapshot.json — a committed VokeSnapshot representing a small but representative Apideck-shaped surface. Do NOT hit the live server (live capture is deferred to Phase 4); construct a synthetic-but-realistic fixture so the determinism test has stable committed input.

    Top-level structure (matches ARCHITECTURE.md VokeSnapshot + D-01/D-02/D-03): snapshotVersion "1", mtqsVersion "0.1", server { url null, name "apideck-fixture", version "0.0.0", protocolVersion "2026-07-28" }, meta { capturedAt "2026-06-12T00:00:00.000Z" }, tools [ sorted ascending by toolId ].

    Include at least 6 tools spanning real Apideck patterns and the spec worked examples:
    - crm_search_contacts (well-designed; nested object inputSchema — copy from spec/MTQS-v0.1.md ~lines 1136-1160)
    - search (poorly-designed; bare q: {} property — copy from spec ~lines 1100-1114)
    - one tool with oneOf/allOf composition in inputSchema (OpenAPI-generated style)
    - one tool with an internal $ref to $defs (D-07 — keep "$ref": "#/$defs/Address" as-written)
    - one tool with outputSchema present
    - one tool with annotations { readOnlyHint true }

    Each tool object includes: toolId (= name), name, description, inputSchema, optional outputSchema, optional annotations, and contentHash. Compute each contentHash with the toolContentHash helper from Task 2 (write a throwaway node script that imports the helper, prints hashes, paste the real hex into the JSON). Tools array MUST be sorted ascending by toolId.

    The fixture must be valid JSON and must NOT contain any scores, findings, or tiers (D-01 — raw surface only).

    Commit the fixture (committed input for the Wave 2 determinism test — never regenerated nondeterministically).
  </action>
  <verify>
    <automated>node tests/fixtures/validate-snapshot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/apideck-snapshot.json exists and JSON.parse succeeds
    - it has tools.length >= 6, tools sorted ascending by toolId, and no tool has a score/findings/tier field (write tests/fixtures/validate-snapshot.mjs as a tiny checker that asserts these and exits non-zero on failure)
    - grep finds "#/$defs/" in tests/fixtures/apideck-snapshot.json (internal $ref kept as-written, D-07)
    - grep finds "crm_search_contacts" and "search" toolIds in the fixture
    - every tool has a non-empty contentHash field (64-char hex)
  </acceptance_criteria>
  <done>A committed, valid VokeSnapshot fixture with 6+ representative tools (sorted, with contentHashes, no scores) exists for the Wave 2 determinism test.</done>
</task>

</tasks>

<verification>
- npm test passes for tests/canonicalize/
- The fixture validates: valid JSON, 6+ tools, sorted by toolId, no scores/findings/tiers
- `npm run typecheck` succeeds (the new package references resolve)
</verification>

<success_criteria>
- @voke/linter package installed with pinned stack; root tsconfig references it
- canonicalJson + sha256 + toolContentHash + surfaceContentHash exported and tested (byte-stable, locale-independent)
- tests/fixtures/apideck-snapshot.json committed (6+ tools, sorted, contentHashes, no scores)
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-ingestion-determinism/02-01-SUMMARY.md`
</output>
