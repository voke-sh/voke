---
phase: 02-engine-ingestion-determinism
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - packages/linter/src/ingestion/types.ts
  - packages/linter/src/ingestion/schema-checks.ts
  - packages/linter/src/ingestion/mcp-client.ts
  - packages/linter/src/ingestion/snapshot-reader.ts
  - packages/linter/src/ingestion/snapshot-writer.ts
  - packages/linter/src/errors.ts
  - packages/linter/src/index.ts
  - tests/ingestion/schema-checks.test.ts
  - tests/ingestion/mcp-client.test.ts
  - tests/ingestion/snapshot-reader.test.ts
  - tests/fixtures/external-ref-tool.json
  - tests/fixtures/deep-schema-tool.json
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-05]
user_setup: []

must_haves:
  truths:
    - "A live Streamable-HTTP MCP server connection retrieves the full paginated tools/list surface"
    - "Custom/bearer headers are forwarded to the transport and masked in all serialized output"
    - "A saved VokeSnapshot is read offline with zero network access and reaches the same surface as a live connect"
    - "An external $ref is detected without any outbound HTTP call"
    - "A schema exceeding the hard depth cap is rejected at ingestion; full JSON Schema 2020-12 composition is accepted"
    - "Any pagination page failure aborts the whole ingest (no partial surface)"
  artifacts:
    - path: "packages/linter/src/ingestion/types.ts"
      provides: "ToolSnapshot, VokeSnapshot, ServerIdentity verbatim from ARCHITECTURE.md"
      exports: ["ToolSnapshot", "VokeSnapshot", "ServerIdentity"]
    - path: "packages/linter/src/ingestion/schema-checks.ts"
      provides: "Ajv2020 validity check, schemaDepth, hasExternalRef"
      exports: ["isValidJsonSchema2020", "schemaDepth", "hasExternalRef", "DEPTH_HARD_CAP"]
    - path: "packages/linter/src/ingestion/mcp-client.ts"
      provides: "connectWithFallback + fetchAllTools + ingestLive producing a sorted VokeSnapshot"
      exports: ["ingestLive", "maskHeaders"]
    - path: "packages/linter/src/ingestion/snapshot-reader.ts"
      provides: "offline VokeSnapshot reader (no network)"
      exports: ["readSnapshot"]
  key_links:
    - from: "packages/linter/src/ingestion/mcp-client.ts"
      to: "packages/linter/src/canonicalize/hash.ts"
      via: "toolContentHash on each ingested tool"
      pattern: "toolContentHash"
    - from: "packages/linter/src/ingestion/schema-checks.ts"
      to: "ajv/dist/2020"
      via: "new Ajv2020({ strict: false })"
      pattern: "Ajv2020"
---

<objective>
Build the ingestion layer: the canonical data-model types (ToolSnapshot / VokeSnapshot / ServerIdentity), the schema-safety checks (Ajv2020 validity, bounded depth, external-$ref detection), the live MCP client (Streamable-HTTP primary + SSE fallback, paginated tools/list, header auth + masking, fail-fast), and the offline snapshot reader/writer. Output of both ingestion paths is the same sorted, content-hashed VokeSnapshot the engine consumes.

Purpose: This is the data-acquisition half of the phase (ING-01..05). It produces the canonicalized surface the engine runs against; getting the data model and determinism enforcement points (#1, #5 sort-on-ingest; #6 hash) right here is load-bearing for the whole pipeline.
Output: An ingestion module with live + offline paths, schema-safety checks, and tests (mocked SDK, offline reader, depth/ext-ref fixtures).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-engine-ingestion-determinism/02-CONTEXT.md
@.planning/phases/02-engine-ingestion-determinism/02-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
From packages/linter/src/canonicalize/hash.ts (Plan 01 — use directly, do not reimplement):
  export const sha256: (input: string) => string;
  export const toolContentHash: (tool: { name; description?; inputSchema; outputSchema?; annotations? }) => string;
  export const surfaceContentHash: (tools: ReadonlyArray<{ toolId: string }>) => string;

ToolSnapshot / VokeSnapshot / ServerIdentity (ARCHITECTURE.md lines 127-164 — implement verbatim, with D-02 meta block adjustment):
  interface ServerIdentity { url: string | null; name: string; version: string; protocolVersion: string; }
  interface ToolSnapshot {
    toolId: string;            // = tool.name (D-03)
    contentHash: string;       // SHA-256 of canonical JSON of {name,description,inputSchema,outputSchema,annotations}
    name: string; title?: string; description?: string;
    inputSchema: Record<string, unknown>;       // JSON Schema 2020-12
    outputSchema?: Record<string, unknown>;
    annotations?: Record<string, unknown>;       // readOnlyHint, destructiveHint, idempotentHint, openWorldHint
  }
  interface VokeSnapshot {
    snapshotVersion: '1';
    mtqsVersion: string;
    server: ServerIdentity;
    meta: { capturedAt: string };  // D-02: provenance lives here, excluded from hash + determinism test
    tools: ToolSnapshot[];          // sorted ascending by toolId
  }

MCP SDK construction (RESEARCH.md Patterns 1-3, SDK v1.29.0):
  import { Client } from '@modelcontextprotocol/sdk/client/index.js';
  import { StreamableHTTPClientTransport } from '@modelcontextprotocol/sdk/client/streamableHttp.js';
  import { SSEClientTransport } from '@modelcontextprotocol/sdk/client/sse.js';
  import type { Tool } from '@modelcontextprotocol/sdk/types.js';
  // header auth: new StreamableHTTPClientTransport(url, { requestInit: { headers } })
  // SSE fallback: try streamable connect; on throw, build fresh Client + SSEClientTransport(url, { requestInit: { headers } })
  // pagination: do { const { tools, nextCursor } = await client.listTools({ cursor }, { timeout: 30000 }); ... } while (cursor)

Ajv2020 (RESEARCH.md Pattern 6, D-06):
  import Ajv2020 from 'ajv/dist/2020';
  import addFormats from 'ajv-formats';
  const ajv = new Ajv2020({ strict: false });  addFormats(ajv);
  // isValidJsonSchema2020 = (schema) => ajv.validateSchema(schema as object) === true
  // NEVER wire loadSchema (no network, no SSRF — D-06)

Depth counter (RESEARCH.md Pattern 8, D-04/D-05): depth(node) = 1 + max(child depth); oneOf/anyOf/allOf/if/then/else/not add 0 (recurse branches at same level); properties/items/$defs/additionalProperties add +1. DEPTH_HARD_CAP = 32.

External $ref detector (RESEARCH.md Pattern 7, D-07): walk tree; any $ref string NOT starting with "#" is external → true. Never fetch.

Exit codes (RESEARCH.md Pattern 12): connect=2, auth/401=3, partial-page=4, depth-exceeded=6. Errors carry a distinct exit code.

@voke/core Severity/DimensionId types are reusable; DimensionId is schema|description|naming|parameters|annotations (5 dims, NOT the 7-dim ARCHITECTURE draft).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ingestion types + schema-safety checks (Ajv2020, depth, external-$ref) + typed errors</name>
  <files>packages/linter/src/ingestion/types.ts, packages/linter/src/ingestion/schema-checks.ts, packages/linter/src/errors.ts, tests/ingestion/schema-checks.test.ts</files>
  <read_first>
    - /Users/samir.amzani/Projects/voke/.planning/research/ARCHITECTURE.md (ToolSnapshot/VokeSnapshot/ServerIdentity lines 127-164; determinism points table lines 492-503)
    - /Users/samir.amzani/Projects/voke/.planning/research/PITFALLS.md (external $ref auto-deref ban + depth bound, lines 78-103, 245-269)
    - /Users/samir.amzani/Projects/voke/.planning/phases/02-engine-ingestion-determinism/02-RESEARCH.md (Patterns 6,7,8; Anti-Patterns "new Ajv() vs Ajv2020")
    - /Users/samir.amzani/Projects/voke/packages/linter/src/canonicalize/hash.ts (toolContentHash signature)
    - /Users/samir.amzani/Projects/voke/packages/core/src/registry-types.ts (DimensionId/Severity reuse)
  </read_first>
  <behavior>
    - isValidJsonSchema2020: returns true for a valid 2020-12 schema (incl. unevaluatedProperties/prefixItems), false for a structurally invalid schema; does NOT throw, does NOT make IO
    - schemaDepth: a flat {type:object, properties:{a:{type:string}}} returns depth 2 (root + one property level)
    - schemaDepth: a oneOf/allOf wrapper does NOT add a level — the wrapper branches recurse at the same depth as the wrapper
    - schemaDepth: a 40-level nested-properties schema returns a value >= DEPTH_HARD_CAP and bails (no stack overflow)
    - hasExternalRef: a schema with "$ref":"https://x.com/s.json" returns true; a schema with only "$ref":"#/$defs/X" returns false; NO network call is made
    - VokeError subclasses carry a numeric exitCode (connect 2, auth 3, partialPage 4, depthExceeded 6)
  </behavior>
  <action>
    Create packages/linter/src/ingestion/types.ts: export ServerIdentity, ToolSnapshot, VokeSnapshot EXACTLY as in the interfaces block (note D-02 meta block { capturedAt } — provenance is here, not on the hashed body). These are plain TS interfaces; no Zod here (Zod validation of snapshots-on-read goes in snapshot-reader Task 3).

    Create packages/linter/src/errors.ts: a base `class VokeError extends Error { constructor(message: string, public readonly exitCode: number) { super(message); } }` plus subclasses ConnectError(exitCode 2), AuthError(exitCode 3), PartialPageError(exitCode 4), DepthExceededError(exitCode 6). Each subclass message must be actionable (include URL + cause where available). NOTE: RuleExecutionError (exitCode 5) and ConfigError (exitCode 7) belong to Plan 03 / config — do not add them here unless trivially co-located; keep this file ingestion-focused.

    Create packages/linter/src/ingestion/schema-checks.ts:
      - Instantiate a module-level `const ajv = new Ajv2020({ strict: false }); addFormats(ajv);` (import Ajv2020 from 'ajv/dist/2020', addFormats from 'ajv-formats'). NEVER pass loadSchema.
      - export const isValidJsonSchema2020 = (schema: unknown): boolean => ajv.validateSchema(schema as object) === true;
      - export const DEPTH_HARD_CAP = 32;
      - export const schemaDepth — implement RESEARCH.md Pattern 8 verbatim (COMPOSITION_KEYS = oneOf,anyOf,allOf,if,then,else,not add 0; properties/items/$defs/definitions/additionalProperties add +1; early-bail when current > DEPTH_HARD_CAP).
      - export const hasExternalRef — implement RESEARCH.md Pattern 7 verbatim (any $ref string not starting with "#" → true; depth>64 emergency bail; pure tree walk, no fetch).

    Write tests RED-first: tests/ingestion/schema-checks.test.ts covering every <behavior> bullet for isValidJsonSchema2020, schemaDepth (flat, composition-adds-0, deep-bails), hasExternalRef (external true, internal-only false). Block network in this test file (vi.stubGlobal('fetch', () => { throw new Error('network blocked'); }) in beforeEach) to prove the ext-ref check never fetches.
  </action>
  <verify>
    <automated>npm test -- tests/ingestion/schema-checks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep finds "ajv/dist/2020" and "strict: false" in packages/linter/src/ingestion/schema-checks.ts
    - grep confirms NO "loadSchema" anywhere in packages/linter/src/ (command: grep -r loadSchema packages/linter/src returns nothing)
    - grep finds "DEPTH_HARD_CAP = 32" in schema-checks.ts
    - grep finds export const hasExternalRef and export const schemaDepth in schema-checks.ts
    - grep finds "exitCode" in packages/linter/src/errors.ts with ConnectError/AuthError/PartialPageError/DepthExceededError classes
    - npm test -- tests/ingestion/schema-checks.test.ts exits 0; tests prove ext-ref detection with fetch stubbed to throw
  </acceptance_criteria>
  <done>Data-model types, typed exit-coded errors, and schema-safety checks (Ajv2020 strict:false, depth-bound with hard cap 32, external-$ref detection without IO) exist and pass tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live MCP client — connect-with-fallback, paginated listTools, header masking, fail-fast, sorted VokeSnapshot</name>
  <files>packages/linter/src/ingestion/mcp-client.ts, packages/linter/src/index.ts, tests/ingestion/mcp-client.test.ts</files>
  <read_first>
    - /Users/samir.amzani/Projects/voke/.planning/phases/02-engine-ingestion-determinism/02-RESEARCH.md (Patterns 1,2,3; Pitfalls 3 cursor, 6 SSE headers; D-08/D-09/D-10/D-11)
    - /Users/samir.amzani/Projects/voke/.planning/research/ARCHITECTURE.md (determinism points #1 and #5: sort tools by toolId on ingest)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/ingestion/types.ts (VokeSnapshot/ToolSnapshot — Task 1)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/ingestion/schema-checks.ts (schemaDepth, DEPTH_HARD_CAP, DepthExceededError — Task 1)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/canonicalize/hash.ts (toolContentHash — Plan 01)
  </read_first>
  <behavior>
    - fetchAllTools: loops on nextCursor (do/while on cursor truthiness, Pitfall 3) and collects all pages
    - fetchAllTools: if any listTools page rejects, the whole call throws a PartialPageError (D-10) — never returns a partial list
    - ingestLive: maps SDK Tool[] to ToolSnapshot[] with toolId=name and contentHash=toolContentHash(tool); sorts tools ascending by toolId via localeCompare('en',{sensitivity:'variant'}) (determinism #1/#5)
    - ingestLive: rejects with DepthExceededError if any tool inputSchema schemaDepth > DEPTH_HARD_CAP (D-04)
    - connectWithFallback: tries StreamableHTTPClientTransport; on connect throw, retries with SSEClientTransport; both transports receive requestInit.headers (Pitfall 6); if both fail, throws ConnectError (exitCode 2)
    - maskHeaders: replaces every header value with "[MASKED]" for keys the user supplied and for any Authorization header (D-09); raw values never appear in the returned VokeSnapshot or any logged string
  </behavior>
  <action>
    Create packages/linter/src/ingestion/mcp-client.ts implementing RESEARCH.md Patterns 1-3:

      - export const buildHeaders = (rawHeaders: string[]): Record<string,string> — split each "Key: Value" on the first ":" (RESEARCH Pattern 1); throw on a header with no ":".
      - export const maskHeaders = (headers: Record<string,string>): Record<string,string> — return a copy with every value replaced by "[MASKED]" (D-09). Use this for anything that enters the VokeSnapshot/server identity or logs. Never store raw header values on ServerIdentity.
      - const connectWithFallback = async (url: URL, headers: Record<string,string>): try { new Client({name:'voke',version:VOKE_VERSION}); new StreamableHTTPClientTransport(url, { requestInit: { headers } }); await client.connect(transport); return {client,transport}; } catch { build fresh Client + new SSEClientTransport(url, { requestInit: { headers } }); await client.connect(...); } — if the SSE attempt also throws, throw new ConnectError(`Failed to connect to ${url}: ${cause}`). (Define VOKE_VERSION as a local const '0.0.0' for now; Phase 4 wires the real version.)
      - const fetchAllTools = async (client, timeoutMs = 30000): do/while on cursor; wrap the listTools await in try/catch that throws new PartialPageError(`tools/list page failed: ${cause}`) (D-10). Default timeout 30000ms hardcoded for Phase 2 (Phase 4 exposes --timeout).
      - export const ingestLive = async (opts: { url: string; rawHeaders?: string[] }): Promise<VokeSnapshot> — orchestrate: buildHeaders → connectWithFallback → read server identity from the SDK's initialize result (client.getServerVersion()/serverInfo for name+version; negotiated protocolVersion) → fetchAllTools → map each Tool to ToolSnapshot (toolId=name, contentHash=toolContentHash(tool)) → for each tool, if schemaDepth(tool.inputSchema) > DEPTH_HARD_CAP throw new DepthExceededError(...) → sort tools ascending by toolId (localeCompare 'en' sensitivity variant) → return VokeSnapshot { snapshotVersion:'1', mtqsVersion:'0.1', server:{url, name, version, protocolVersion}, meta:{ capturedAt: new Date().toISOString() }, tools }. capturedAt is the ONLY wall-clock value and lives in meta (D-02). Do NOT store raw headers anywhere on the snapshot.

    Add ingestLive + maskHeaders to packages/linter/src/index.ts barrel (with .js extensions).

    Write tests RED-first: tests/ingestion/mcp-client.test.ts. Mock the SDK Client (vi.mock('@modelcontextprotocol/sdk/client/index.js')) so no real network occurs. Cover: pagination across 2+ pages collects all tools; a rejecting page throws PartialPageError; ingestLive output tools are sorted by toolId; maskHeaders replaces values with [MASKED] and the serialized VokeSnapshot contains no raw token; a depth-exceeding inputSchema throws DepthExceededError. Stub fetch to throw to prove no accidental real network.
  </action>
  <verify>
    <automated>npm test -- tests/ingestion/mcp-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep finds StreamableHTTPClientTransport and SSEClientTransport in packages/linter/src/ingestion/mcp-client.ts
    - grep finds "requestInit" and "headers" on BOTH transport constructions (SSE fallback forwards headers, Pitfall 6)
    - grep finds "[MASKED]" in mcp-client.ts (maskHeaders) and grep finds export const ingestLive and export const maskHeaders
    - grep finds PartialPageError thrown in the listTools loop and DepthExceededError in the depth check
    - grep finds localeCompare('en', { sensitivity: 'variant' }) on the tools sort in ingestLive
    - npm test -- tests/ingestion/mcp-client.test.ts exits 0; tests prove pagination, partial-page abort, sort, masking (no raw token in serialized output), and depth rejection — all with the SDK mocked and fetch stubbed
  </acceptance_criteria>
  <done>ingestLive produces a sorted, content-hashed VokeSnapshot from a live (mocked) server with header auth+masking, SSE fallback, pagination, fail-fast on partial page, and hard-depth rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offline snapshot reader + writer (ING-03) with external-$ref and deep-schema fixtures</name>
  <files>packages/linter/src/ingestion/snapshot-reader.ts, packages/linter/src/ingestion/snapshot-writer.ts, packages/linter/src/index.ts, tests/ingestion/snapshot-reader.test.ts, tests/fixtures/external-ref-tool.json, tests/fixtures/deep-schema-tool.json</files>
  <read_first>
    - /Users/samir.amzani/Projects/voke/.planning/phases/02-engine-ingestion-determinism/02-CONTEXT.md (D-01 VokeSnapshot raw surface; ING-03 offline, no network)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/ingestion/types.ts (VokeSnapshot — Task 1)
    - /Users/samir.amzani/Projects/voke/packages/core/src/registry-types.ts (zod usage pattern for parse-on-read)
    - /Users/samir.amzani/Projects/voke/packages/linter/src/canonicalize/canonical-json.ts (writer serializes via canonicalJson)
    - /Users/samir.amzani/Projects/voke/tests/fixtures/apideck-snapshot.json (Plan 01 fixture — reader reads this)
  </read_first>
  <behavior>
    - readSnapshot: reads a VokeSnapshot JSON file from disk via node:fs and returns a typed VokeSnapshot; makes ZERO network calls
    - readSnapshot: rejects (throws) a malformed snapshot that fails a Zod shape check (clear error message)
    - readSnapshot on the Plan 01 apideck fixture yields tools already sorted by toolId (reader does not re-sort but asserts/relies on committed order)
    - writeSnapshot: serializes a VokeSnapshot to disk; the body (excluding meta.capturedAt) is canonical/sorted so two writes of the same surface are byte-identical
    - external-ref fixture: hasExternalRef returns true; deep-schema fixture: schemaDepth exceeds DEPTH_HARD_CAP
  </behavior>
  <action>
    Create packages/linter/src/ingestion/snapshot-reader.ts:
      - Define a Zod schema VokeSnapshotSchema (mirror the VokeSnapshot interface; tools array of a ToolSnapshotSchema with toolId/contentHash/name/inputSchema required). Reuse zod (already a dep).
      - export const readSnapshot = (path: string): VokeSnapshot => { const raw = readFileSync(path, 'utf8'); const parsed = JSON.parse(raw); return VokeSnapshotSchema.parse(parsed) as VokeSnapshot; }; — synchronous node:fs only, NO network, NO SDK import. This guarantees ING-03 offline.

    Create packages/linter/src/ingestion/snapshot-writer.ts:
      - export const writeSnapshot = (path: string, snapshot: VokeSnapshot): void — serialize the tool surface body via canonicalJson (sorted keys) and write with node:fs writeFileSync. Keep meta.capturedAt as written (it is provenance, D-02) but ensure the tools body is canonical so re-writes of an identical surface are byte-identical.

    Add readSnapshot + writeSnapshot to the barrel (index.ts, .js extensions).

    Create two committed fixtures for ING-05:
      - tests/fixtures/external-ref-tool.json: a single tool whose inputSchema contains "$ref": "https://malicious.example.com/schema.json" (external — must be flagged, never fetched).
      - tests/fixtures/deep-schema-tool.json: a single tool whose inputSchema nests >32 levels of properties (exceeds DEPTH_HARD_CAP). Generate the nesting programmatically in a throwaway script, then commit the resulting JSON.

    Write tests RED-first: tests/ingestion/snapshot-reader.test.ts. Cover: readSnapshot on tests/fixtures/apideck-snapshot.json returns a snapshot with tools.length>=6 and no network (stub fetch to throw, assert no throw from reader); readSnapshot on a deliberately malformed object throws a Zod error; hasExternalRef(external-ref-tool inputSchema) === true with fetch stubbed to throw (proves no fetch); schemaDepth(deep-schema-tool inputSchema) > DEPTH_HARD_CAP.
  </action>
  <verify>
    <automated>npm test -- tests/ingestion/snapshot-reader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep finds export const readSnapshot in snapshot-reader.ts and it imports node:fs (readFileSync) and zod, NOT @modelcontextprotocol/sdk
    - grep -L @modelcontextprotocol snapshot-reader.ts confirms the reader has no SDK import (offline guarantee)
    - grep finds export const writeSnapshot in snapshot-writer.ts and it imports canonicalJson
    - tests/fixtures/external-ref-tool.json contains "https://" inside a $ref; tests/fixtures/deep-schema-tool.json parses as JSON
    - npm test -- tests/ingestion/snapshot-reader.test.ts exits 0; proves offline read (fetch stubbed to throw, reader still works), Zod rejection of malformed input, external-ref detection, and deep-schema over-cap
  </acceptance_criteria>
  <done>Offline snapshot reader (Zod-validated, no network/SDK) and canonical writer exist; ING-05 external-ref + deep-schema fixtures are committed and exercised by tests.</done>
</task>

</tasks>

<verification>
- npm test passes for tests/ingestion/ (schema-checks, mcp-client, snapshot-reader)
- grep -r loadSchema packages/linter/src returns nothing (D-06 — never wired)
- readSnapshot performs zero network/SDK calls (offline ING-03)
- `npm run typecheck` succeeds
</verification>

<success_criteria>
- ING-01: paginated tools/list collected via Streamable-HTTP (+ SSE fallback), mocked in test
- ING-02: header auth forwarded to both transports; values masked in all output
- ING-03: offline snapshot reader returns the same surface with no network
- ING-05: external-$ref detected without fetch; depth bounded with hard cap 32; full 2020-12 accepted (Ajv2020 strict:false)
- Determinism #1/#5/#6: tools sorted by toolId on ingest; contentHash per tool
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine-ingestion-determinism/02-02-SUMMARY.md`
</output>
