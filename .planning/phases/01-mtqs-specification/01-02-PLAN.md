---
phase: 01-mtqs-specification
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - spec/mtqs-v0.1.yaml
  - tests/spec/registry.schema.test.ts
  - tests/spec/registry.rules.test.ts
autonomous: true
requirements: [SPEC-03, SPEC-04]
must_haves:
  truths:
    - "A machine-readable YAML registry exists with exactly 20 entries, one per v0.1 rule"
    - "Every entry validates against the RuleRegistryEntrySchema Zod type (SPEC-04 build gate)"
    - "Every rule has a stable namespaced ID (MTQS-[SDNPA]NN), a severity, a dimension, a scope, a weight, a non-empty non-Glama source citation, and a fix hint (SPEC-03 + SPEC-01 source field)"
    - "No duplicate rule IDs; all 20 expected IDs present"
  artifacts:
    - path: "spec/mtqs-v0.1.yaml"
      provides: "the single-source-of-truth rule registry for spec + linter"
      contains: "MTQS-S01"
    - path: "tests/spec/registry.schema.test.ts"
      provides: "Zod validation gate over the registry"
    - path: "tests/spec/registry.rules.test.ts"
      provides: "20-rule completeness + uniqueness + source-non-Glama checks"
  key_links:
    - from: "tests/spec/registry.schema.test.ts"
      to: "spec/helpers/loadRegistry.ts"
      via: "loadRegistryFile('spec/mtqs-v0.1.yaml')"
      pattern: "loadRegistryFile"
    - from: "spec/mtqs-v0.1.yaml"
      to: "spec/registry-types.ts"
      via: "validated by RuleRegistrySchema"
      pattern: "MTQS-[SDNPA]"
---

<objective>
Author `spec/mtqs-v0.1.yaml` — the machine-readable rule registry that is the single source of truth (D-10) for all 20 v0.1 MTQS rules. This locks rule IDs, severities, dimensions, scopes, weights, source citations, and fix hints BEFORE any prose is written (Pitfall 1: never write rubric prose before the registry is locked). Add the two tests that gate it (SPEC-04 schema validation + SPEC-03 completeness / uniqueness / source).

Purpose: The registry is the contract. The spec doc (Plan 03) and the Phase 2 linter both consume it. Authoring it first prevents spec-vs-registry drift (Pitfall 6).
Output: spec/mtqs-v0.1.yaml (20 entries) + registry.schema.test.ts + registry.rules.test.ts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-mtqs-specification/01-CONTEXT.md
@.planning/phases/01-mtqs-specification/01-RESEARCH.md
@.planning/research/FEATURES.md

<interfaces>
<!-- From Plan 01 (Wave 0). Use directly. -->
spec/helpers/loadRegistry.ts:
  loadRegistry(yamlText: string): RuleRegistryEntry[]
  loadRegistryFile(path: string): RuleRegistryEntry[]
spec/registry-types.ts:
  RuleRegistryEntrySchema  (id /^MTQS-[SDNPA]\d{2}$/, severity, dimension, scope, weight 0.1–3.0, description>=10, fixHint>=10, source URL, mtqsVersion /^\d+\.\d+$/)
  RuleRegistrySchema = { rules: RuleRegistryEntry[] }

Required YAML field set per entry (ALL required):
  id, severity, dimension, scope, weight, description, fixHint, source, mtqsVersion

Dimension multipliers (the weight field value): schema=1.5, annotations=1.5, description=1.2, parameters=1.2, naming=1.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 20-rule YAML registry (spec/mtqs-v0.1.yaml)</name>
  <files>spec/mtqs-v0.1.yaml</files>
  <read_first>
    - .planning/research/FEATURES.md ("Table Stakes" tables for S01–S08, D01–D03, N01–N03, P01–P02, and the A01–A06 rows in the Annotations table — these give check, severity, fix hint, source per rule)
    - .planning/phases/01-mtqs-specification/01-RESEARCH.md ("Primary Source Citations" for exact URLs per dimension; "YAML Registry Schema" → Field Specification + Sample Entries; Open Questions for S05 depth=5, N01 length thresholds)
    - .planning/phases/01-mtqs-specification/01-CONTEXT.md (D-04 severity ladder, D-09 weight tiers)
    - spec/registry-types.ts (the Zod schema the YAML must satisfy)
  </read_first>
  <action>
    Create `spec/mtqs-v0.1.yaml` with a top-level `rules:` array of EXACTLY 20 entries — these and only these IDs (the v0.1 P1 set from FEATURES.md MVP; P2 rules A07–A09, D04–D06, P03–P05, O01–O04, N04–N06, C01–C03 are OUT OF SCOPE — do NOT include them).

    The 20 entries, each with id / severity / dimension / scope / weight, plus a >=10-char description (one sentence from the FEATURES.md Check column) and a >=10-char fixHint (verbatim/adapted from the FEATURES.md Fix Hint column) and a real https source URL (never Glama) and mtqsVersion "0.1":

    Schema (dimension: schema, weight: 1.5, scope: per-tool):
      MTQS-S01 error   — inputSchema present and non-null. source: https://modelcontextprotocol.io/specification/draft/basic/index#json-schema-usage
      MTQS-S02 error   — inputSchema root has type:object. source: https://modelcontextprotocol.io/specification/draft/basic/index#json-schema-usage
      MTQS-S03 error   — inputSchema is structurally valid JSON Schema 2020-12. source: https://json-schema.org/draft/2020-12
      MTQS-S04 error   — no unresolved external $ref (refs outside $defs). source: https://modelcontextprotocol.io/specification/draft/basic/index#json-schema-usage
      MTQS-S05 warning — schema nesting depth <= 5; label this MTQS-RECOMMENDED (not MCP-mandated) in description + fixHint. source: https://modelcontextprotocol.io/specification/draft/basic/index#json-schema-usage
      MTQS-S06 error   — outputSchema, if present, is valid JSON Schema 2020-12. source: https://json-schema.org/draft/2020-12
      MTQS-S07 warning — required array present when properties defined (even []). source: https://json-schema.org/draft/2020-12
      MTQS-S08 warning — no property uses a bare {} untyped schema (each has type or $ref). source: https://json-schema.org/draft/2020-12

    Description (dimension: description, weight: 1.2, scope: per-tool):
      MTQS-D01 error   — description present and non-empty. source: https://www.anthropic.com/engineering/writing-tools-for-agents
      MTQS-D02 warning — description length >= 20 chars. source: https://www.anthropic.com/engineering/writing-tools-for-agents
      MTQS-D03 error   — description is not a byte-for-byte copy of name. source: https://www.anthropic.com/engineering/writing-tools-for-agents

    Naming (dimension: naming, weight: 1.0):
      MTQS-N01 error — scope per-tool — name present, non-empty, 1–128 chars (error if >128; fixHint notes SEP-986 recommends 1–64). source: https://modelcontextprotocol.io/seps/986-specify-format-for-tool-names
      MTQS-N02 error — scope per-tool — name matches [A-Za-z0-9_\-./] only (no spaces/commas). source: https://modelcontextprotocol.io/seps/986-specify-format-for-tool-names
      MTQS-N03 error — scope server — tool names unique within the server. source: https://modelcontextprotocol.io/seps/986-specify-format-for-tool-names

    Parameters (dimension: parameters, weight: 1.2, scope: per-tool):
      MTQS-P01 warning — every property in inputSchema.properties has a non-empty description. source: https://www.anthropic.com/engineering/writing-tools-for-agents
      MTQS-P02 warning — finite-string-set properties use enum. source: https://www.anthropic.com/engineering/writing-tools-for-agents

    Annotations (dimension: annotations, weight: 1.5, scope: per-tool):
      MTQS-A01 info    — annotations object present. source: https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/schema/draft/schema.ts
      MTQS-A02 warning — readOnlyHint explicitly set (boolean). source: https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/schema/draft/schema.ts
      MTQS-A03 warning — destructiveHint explicitly set (boolean). source: https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/schema/draft/schema.ts
      MTQS-A04 info    — idempotentHint explicitly set (boolean). source: https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/schema/draft/schema.ts
      MTQS-A05 info    — openWorldHint explicitly set (boolean). source: https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/schema/draft/schema.ts
      MTQS-A06 error   — when readOnlyHint:true, destructiveHint must not also be true. source: https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/schema/draft/schema.ts

    Counts: 8 schema + 3 description + 3 naming + 2 parameters + 6 annotations = 20.

    Use the YAML block-scalar `>` form for multi-line description / fixHint exactly as the Sample Entries in 01-RESEARCH.md show (MTQS-S03, MTQS-A06, MTQS-D02 samples). Use the listed severity for each ID exactly — do NOT add or remove rules to balance severities.
  </action>
  <verify>
    <automated>cd /Users/samir.amzani/Projects/voke && grep -c 'id: MTQS-' spec/mtqs-v0.1.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id: MTQS-' spec/mtqs-v0.1.yaml` returns exactly 20
    - grep finds each of these IDs exactly once: MTQS-S01..S08, MTQS-D01..D03, MTQS-N01..N03, MTQS-P01..P02, MTQS-A01..A06
    - grep finds no P2 IDs (no MTQS-A07, A08, A09, D04, D05, D06, P03, P04, P05, O01-O04, N04-N06, C01-C03)
    - grep -i "glama" spec/mtqs-v0.1.yaml returns nothing (zero matches)
    - `grep -c 'mtqsVersion' spec/mtqs-v0.1.yaml` returns 20
    - MTQS-N03 entry has `scope: server`; all other entries have `scope: per-tool`
  </acceptance_criteria>
  <done>20 entries present with the exact IDs above; no P2 rules; no Glama references; N03 is server-scoped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry schema-validation + completeness/uniqueness tests</name>
  <files>tests/spec/registry.schema.test.ts, tests/spec/registry.rules.test.ts</files>
  <read_first>
    - .planning/phases/01-mtqs-specification/01-VALIDATION.md (Validation Checks table: registry.schema → Zod; registry.rules → 20 present, IDs unique, source non-empty + non-Glama)
    - spec/helpers/loadRegistry.ts (loadRegistryFile signature)
    - spec/registry-types.ts (RuleRegistryEntrySchema)
    - spec/mtqs-v0.1.yaml (the registry under test)
  </read_first>
  <behavior>
    registry.schema.test.ts:
    - Test 1: loadRegistryFile('spec/mtqs-v0.1.yaml') returns without throwing (every entry passes Zod RuleRegistryEntrySchema → SPEC-04 gate).
    registry.rules.test.ts:
    - Test 2: registry has exactly 20 entries.
    - Test 3: the set of ids equals the exact expected set [MTQS-S01..S08, D01..D03, N01..N03, P01..P02, A01..A06] (no missing, no extra).
    - Test 4: no duplicate ids.
    - Test 5: every id matches /^MTQS-[SDNPA]\d{2}$/.
    - Test 6: every entry's source is a non-empty string and does NOT contain "glama" (case-insensitive) — anti-Glama SPEC-01 guard.
    - Test 7: every entry has a non-empty fixHint (>= 10 chars).
    - Test 8: weights match the dimension tier: schema & annotations → 1.5, description & parameters → 1.2, naming → 1.0.
  </behavior>
  <action>
    Create `tests/spec/registry.schema.test.ts`:
    - import { loadRegistryFile } from the helper; in a test, call it on 'spec/mtqs-v0.1.yaml' inside `expect(() => loadRegistryFile(path)).not.toThrow()`.
    - Resolve the path relative to the repo root (use process.cwd() or a path relative to import.meta.url) so it works under `vitest run`.

    Create `tests/spec/registry.rules.test.ts`:
    - Load the registry once at top (const rules = loadRegistryFile('spec/mtqs-v0.1.yaml')).
    - Define EXPECTED_IDS as the literal 20-element array above.
    - Implement Tests 2–8 exactly as the behaviors describe. Use a Set for uniqueness, a weight-by-dimension lookup map { schema:1.5, annotations:1.5, description:1.2, parameters:1.2, naming:1.0 } for Test 8.
    - No `any`; import RuleRegistryEntry type for annotations.
  </action>
  <verify>
    <automated>cd /Users/samir.amzani/Projects/voke && npx vitest run tests/spec/registry.schema.test.ts tests/spec/registry.rules.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/spec/registry.schema.test.ts tests/spec/registry.rules.test.ts` exits 0
    - registry.rules.test.ts asserts exactly 20 entries and the exact EXPECTED_IDS set
    - a test asserts no entry source contains "glama" (case-insensitive)
    - a test asserts weight-by-dimension (schema/annotations 1.5, description/parameters 1.2, naming 1.0)
    - grep confirms no `: any` / `as any` in either test file
  </acceptance_criteria>
  <done>Both registry tests pass; the 20-rule set is complete, unique, schema-valid, non-Glama, and correctly weighted.</done>
</task>

</tasks>

<verification>
- `npx vitest run spec/` (i.e. the spec tests) green: registry parses against Zod, all 20 rules present + unique, sources non-Glama, weights correct.
- `grep -c 'id: MTQS-' spec/mtqs-v0.1.yaml` == 20.
</verification>

<success_criteria>
- spec/mtqs-v0.1.yaml is the locked single source of truth: 20 entries, 9 required fields each, valid against RuleRegistryEntrySchema.
- SPEC-03 satisfied: stable IDs + severities for all 20 rules.
- SPEC-04 satisfied: the Zod gate fails the build on any malformed/missing-field entry; the registry passes it.
- SPEC-01 source field satisfied: every rule cites a primary (non-Glama) source.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mtqs-specification/01-02-SUMMARY.md` documenting: the final 20-rule ID/severity/dimension/weight table, the source URL used per dimension, any wording chosen for description/fixHint, and confirmation that N03 is the only server-scoped rule.
</output>
