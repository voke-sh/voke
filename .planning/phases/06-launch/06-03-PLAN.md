---
phase: 06-launch
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - .planning/phases/06-launch/blog-launch-post.md
autonomous: false
requirements: [PUB-03]
must_haves:
  truths:
    - "a launch blog post draft exists in markdown telling the 'no open standard for MCP tool quality — here is one' story"
    - "the post follows the D-05 narrative shape (problem → deterministic beats LLM-judge → MTQS in 22 rules → live proof → try-it CTA)"
    - "the post cites the REAL captured Apideck + DeepWiki scores verbatim (D-08), not assumed numbers"
    - "the post's CTA uses the exact published package/command verified in Plan 02"
    - "the draft passes a stop-slop pass (score >=35/50, no em dashes, no AI tells) before publish"
    - "the post is published to dev.to and the live URL is reachable"
  artifacts:
    - path: ".planning/phases/06-launch/blog-launch-post.md"
      provides: "The launch blog post markdown source (the deliverable; pasted to dev.to)"
      min_lines: 60
  key_links:
    - from: "blog-launch-post.md CTA"
      to: "@voke-sh/voke@<published-version> (from Plan 02 SUMMARY)"
      via: "npx command + Action snippet in the try-it section"
      pattern: "@voke-sh/voke"
    - from: "blog-launch-post.md proof section"
      to: "real Apideck + DeepWiki scores (from Plan 01 + Plan 02 SUMMARYs)"
      via: "verbatim score + tier + contentHash determinism receipt"
      pattern: "Tier"
---

<objective>
Write and publish the launch blog post (PUB-03): the "no open standard for MCP tool quality — here is one" story, with the live Apideck run as the proof artifact. Venue is dev.to (D-04, NOT the Apideck Contentful blog — employer-conflict boundary).

D-11 ORDERING: this plan depends on Plan 02 — npm published + npx verified — because the post's CTA cites the live `npx @voke-sh/voke` command and the post's proof section cites the real verified scores. Blog goes out only after the package works.

Purpose: PUB-03 launch story. The honest D-tier Apideck score (employer's own server scoring poorly) is the neutrality hook and the credibility argument vs Glama's closed LLM-judge.
Output: a published dev.to post + its markdown source committed under the phase dir.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-launch/06-CONTEXT.md
@.planning/phases/06-launch/06-RESEARCH.md

# Real numbers + verified CTA come from the prior plans (READ both SUMMARYs):
@.planning/phases/06-launch/06-01-SUMMARY.md
@.planning/phases/06-launch/06-02-SUMMARY.md

# Story / positioning source material:
@spec/MTQS-v0.1.md
@spec/SCOPE.md
@README.md

# Writing standards (skills — load before drafting/publishing):
@~/.claude/skills/blog/SKILL.md
@~/.claude/skills/stop-slop/SKILL.md

<interfaces>
<!-- Constraints the executor must honor. -->
- Venue: dev.to (D-04). Do NOT use the blog skill's /blog publish (Contentful) flow — that targets the Apideck engineering blog (employer-conflict line). Use ONLY the blog skill's WRITING STANDARDS section.
- Narrative shape (D-05): Problem → why deterministic beats LLM-judge → MTQS in 22 rules → live proof (Apideck + DeepWiki) → try-it CTA (npx + Action snippet).
- Scores (D-08): use the VERBATIM captured numbers from 06-01-SUMMARY / 06-02-SUMMARY. Do NOT hard-code 62/D from memory — use the real recorded value.
- Determinism receipt (D-09): show the SHA-256 contentHash / canonical-JSON byte equality as the "same input → same score every run" proof. Read contentHash straight from tests/fixtures/apideck-live-snapshot.json.
- CTA package/command: the EXACT published version verified in 06-02-SUMMARY (e.g. @voke-sh/voke@0.1.0) and the Action ref `uses: voke-sh/voke@v0`.
- Scope guard (spec/SCOPE.md): claims stay inside L1 — no LLM-in-loop, no gateway/proxy, no L2+.
- stop-slop (D-06): mandatory pre-publish pass; score >=35/50; no em dashes, no filler, active voice.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the launch post markdown (D-05 shape, real numbers, scoped claims)</name>
  <files>.planning/phases/06-launch/blog-launch-post.md</files>
  <read_first>
    - .planning/phases/06-launch/06-01-SUMMARY.md (verbatim Apideck + DeepWiki captured scores)
    - .planning/phases/06-launch/06-02-SUMMARY.md (verified published version + verbatim live score/tier for the CTA)
    - spec/MTQS-v0.1.md (§1 motivation, §2 dimensions/weights, §4 scoring formula — source for the "22 rules" section)
    - spec/SCOPE.md (L1 boundary — keep claims in scope)
    - README.md (CTA must match what actually ships: npx + Action snippet)
    - ~/.claude/skills/blog/SKILL.md (§"Writing Standards" ONLY — code examples runnable, direct voice, the avoid-list)
    - tests/fixtures/apideck-live-snapshot.json (read a couple of tool contentHash values for the D-09 determinism receipt)
  </read_first>
  <action>
    Write the post to .planning/phases/06-launch/blog-launch-post.md as dev.to-ready markdown, following the D-05 shape exactly:

    1. PROBLEM — there is no open, reproducible standard for MCP tool quality; the incumbent (Glama) is a closed, non-reproducible LLM-judge score.
    2. WHY DETERMINISTIC BEATS LLM-JUDGE — same input → same output, gradeable in CI, no model in the loop. This is the wedge.
    3. MTQS IN 22 RULES — summarize the 5 dimensions + weights from spec §2 (schema=1.5, annotations=1.5, description=1.2, parameters=1.2, naming=1.0) and the A-F tier scoring from §4. Link to voke-sh.github.io/voke/spec/ (interim; note voke.sh/spec as target).
    4. LIVE PROOF — run voke against Apideck (the employer's own server) and DeepWiki. Use the VERBATIM scores from 06-01/06-02 SUMMARYs (do NOT assume 62/D — use the recorded number). The honest D-tier on the employer's server is the neutrality hook (D-08). Show the determinism receipt (D-09): same tool contentHash → same score across 3 runs.
    5. TRY-IT CTA — the exact verified command from 06-02-SUMMARY:
       ```
       npx -y @voke-sh/voke@<version> lint https://mcp.apideck.dev/mcp
       ```
       plus the GitHub Action snippet (uses: voke-sh/voke@v0 with min-score) lifted from README.

    Code blocks: runnable, real commands only (blog skill standard). Set dev.to front-matter (title, published: false, tags, and a canonical_url decision per RESEARCH Pitfall 6 — document the choice inline).

    Keep every claim inside spec/SCOPE.md's L1 boundary. Do NOT reference Glama's actual scoring numbers or derive from them (hard owner rule).
  </action>
  <verify>
    <automated>test -f .planning/phases/06-launch/blog-launch-post.md && grep -q "@voke-sh/voke" .planning/phases/06-launch/blog-launch-post.md && grep -qi "Tier" .planning/phases/06-launch/blog-launch-post.md && echo DRAFT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-launch/blog-launch-post.md` exists, >=60 lines
    - Contains all 5 D-05 sections (problem, deterministic-vs-LLM, 22 rules, live proof, CTA) — verifiable by section headings
    - CTA contains the exact published command `npx -y @voke-sh/voke@<version> lint` matching 06-02-SUMMARY (grep "@voke-sh/voke")
    - Cites the real Apideck score+tier from 06-01/06-02 SUMMARY verbatim (grep the recorded number) — NOT a placeholder
    - Contains a determinism receipt referencing contentHash / byte-identical (grep "contentHash" or "byte-identical")
    - Mentions DeepWiki as the second server with its real score
    - No reference to Contentful / Apideck engineering blog publish flow
  </acceptance_criteria>
  <done>dev.to-ready draft exists following D-05 shape with real verified numbers and a working CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Run the mandatory stop-slop pass and revise (D-06)</name>
  <files>.planning/phases/06-launch/blog-launch-post.md</files>
  <read_first>
    - ~/.claude/skills/stop-slop/SKILL.md (the 8 core rules, quick checks, 5-dimension scoring; below 35/50 = revise)
    - .planning/phases/06-launch/blog-launch-post.md (the draft from Task 1)
  </read_first>
  <action>
    Apply the stop-slop skill to blog-launch-post.md. Run the Quick Checks list: kill adverbs, fix passive voice, remove em dashes, restructure Wh-word sentence openers, cut "here's what/this" throat-clearing, replace "not X, it's Y" contrasts with direct statements, vary sentence rhythm, name specifics instead of vague declaratives.

    Score the revised draft 1-10 on each of the 5 dimensions (Directness, Rhythm, Trust, Authenticity, Density). If total < 35/50, revise again until >= 35/50. Record the final score in the SUMMARY.

    Hard constraints (also project-wide per prior em-dash cleanup quick task): ZERO em dashes in the final draft.
  </action>
  <verify>
    <automated>! grep -q "—" .planning/phases/06-launch/blog-launch-post.md && echo NO_EMDASH_OK</automated>
  </verify>
  <acceptance_criteria>
    - No em dash character (—) anywhere in the file (grep returns nothing)
    - The 5-dimension stop-slop score is recorded and totals >= 35/50
    - No banned AI-tell phrases present: grep -iE "furthermore|moreover|in conclusion|cutting-edge|seamless|game-changer" returns nothing
  </acceptance_criteria>
  <done>Draft passes stop-slop at >=35/50 with zero em dashes and no AI-tell phrases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Publish to dev.to and confirm the live URL</name>
  <what-built>
    A stop-slop-clean launch post at .planning/phases/06-launch/blog-launch-post.md following the D-05 narrative, citing the real verified Apideck + DeepWiki scores and the working npx CTA.
  </what-built>
  <action>
    Publish the post to dev.to via manual paste (D-04, lowest-risk for a one-shot launch post). Open dev.to → Create Post, paste the full blog-launch-post.md, set tags (mcp, opensource, devtools, ai) and the canonical_url per the Task 1 decision. Before publishing, re-run the CTA command `npx -y @voke-sh/voke@<version> lint https://mcp.apideck.dev/mcp` to confirm it still works (D-11). Publish, then record the live dev.to URL in the SUMMARY. Do NOT use the blog skill's Contentful /blog publish flow.
  </action>
  <verify>Live dev.to URL loads AND the published CTA command exits 0 with a score+tier AND cited scores match Plan 01/02 SUMMARY</verify>
  <done>Post live on dev.to with a working CTA and real scores; live URL recorded.</done>
  <read_first>
    - .planning/phases/06-launch/blog-launch-post.md (the final draft to paste)
    - .planning/phases/06-launch/06-RESEARCH.md (Open Q #4 — manual paste recommended over API; Pitfall 6 canonical_url)
  </read_first>
  <how-to-verify>
    Manual publish (D-04, RESEARCH Open Q #4 recommends manual paste as lowest-risk for a one-shot launch post):

    1. Open dev.to -> Create Post.
    2. Paste the full contents of .planning/phases/06-launch/blog-launch-post.md.
    3. Set tags (e.g. mcp, opensource, devtools, ai) and the canonical_url per the choice documented in Task 1.
    4. Before publishing, copy-run the post's CTA command yourself to confirm it still works:
       `npx -y @voke-sh/voke@<version> lint https://mcp.apideck.dev/mcp`
       (D-11: the live CTA must work at the moment of publish.)
    5. Publish. Copy the live dev.to URL.

    Confirm: the live URL loads, the Apideck/DeepWiki numbers shown match 06-01/06-02 SUMMARY, and the CTA command in the published post runs green.
  </how-to-verify>
  <acceptance_criteria>
    - The post is live on dev.to and the URL is reachable (loads in a browser)
    - The published CTA command runs green (npx lint exits 0 with a score+tier) at publish time
    - The live dev.to URL is recorded in the SUMMARY
    - The post's cited scores match the captured-fixture scores from Plan 01/02
  </acceptance_criteria>
  <resume-signal>Type "published" with the live dev.to URL, or describe what blocked publishing</resume-signal>
</task>

</tasks>

<verification>
- blog-launch-post.md exists, follows D-05 shape, cites real verified numbers, has a working CTA
- stop-slop pass complete: >=35/50, zero em dashes, no AI-tell phrases
- Post published to dev.to; live URL reachable and recorded
- CTA command verified green at publish time (D-11 honored end-to-end)
</verification>

<success_criteria>
- PUB-03 satisfied: launch blog post live on dev.to telling the MTQS story with the live Apideck run as proof
- Real, honest scores published verbatim (D-08); determinism receipt shown (D-09)
- CTA points at the verified published package; no employer-conflict (Contentful/Apideck-blog) path used
</success_criteria>

<output>
After completion, create `.planning/phases/06-launch/06-03-SUMMARY.md` — record the live dev.to URL, the final stop-slop score, and the verbatim scores cited in the post.
</output>
