---
phase: 06-launch
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements: [PUB-04]
user_setup:
  - service: npm
    why: "Publishing @voke-sh/voke@0.1.0 requires the NPM_TOKEN repo secret for publish.yml OIDC provenance publish"
    env_vars:
      - name: NPM_TOKEN
        source: "npmjs.com -> Access Tokens (Automation) -> set as GitHub repo secret NPM_TOKEN"
    dashboard_config:
      - task: "Confirm NPM_TOKEN secret is set on the repo before creating the v0.1.0 release"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "@voke-sh/voke@0.1.0 is live on the public npm registry"
    - "npx -y @voke-sh/voke@0.1.0 --version prints the version + MTQS version"
    - "npx -y @voke-sh/voke@0.1.0 lint https://mcp.apideck.dev/mcp --ci runs end-to-end and prints a score + tier, exit 0"
    - "the v0 moving tag points at the 0.1.0 release commit"
  artifacts: []
  key_links:
    - from: "GitHub Release v0.1.0"
      to: ".github/workflows/publish.yml"
      via: "release published event triggers npm publish --provenance"
      pattern: "on:\\s*release"
    - from: "npx @voke-sh/voke@0.1.0"
      to: "the published dist/cli/index.js"
      via: "npm registry resolution"
      pattern: "@voke-sh/voke@0.1.0"
---

<objective>
Execute the npm release ritual: confirm publish state, create the v0.1.0 GitHub Release that triggers `publish.yml`, then verify the published artifact end-to-end with `npx -y @voke-sh/voke@0.1.0`. This is the live half of PUB-04 (the DoD includes a working `voke lint https://mcp.apideck.dev/mcp`).

Resolves RESEARCH Open Question #2 (is 0.1.0 already published?) and #3 (is NPM_TOKEN set?).

D-11 STRICT ORDERING: npm publish + npx verification MUST complete and pass BEFORE the blog (Plan 03). A dead npx command in the launch-day CTA kills trust. This plan gates Plan 03.

Purpose: ship the package the blog's CTA points at, and prove it works before citing it.
Output: @voke-sh/voke@0.1.0 live on npm, v0 tag moved, npx verification logged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-launch/06-CONTEXT.md
@.planning/phases/06-launch/06-RESEARCH.md

# Release mechanics (READ — reuse, do not rebuild):
@.github/workflows/publish.yml
@README.md

<interfaces>
<!-- The release pipeline already shipped in Phase 5. This plan TRIGGERS it; it does not modify it. -->
publish.yml: on release published → npm ci → npm --workspace @voke-sh/voke run build
  → npm publish --workspace @voke-sh/voke --provenance --access public (uses secrets.NPM_TOKEN + id-token:write)
  → git tag -fa v0 && git push origin v0 --force

Published package name: @voke-sh/voke (scoped — unscoped "voke" is taken).
Action reference (unchanged): uses: voke-sh/voke@v0
package.json already declares "version": "0.1.0".
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Resolve publish state + NPM_TOKEN before tagging</name>
  <read_first>
    - .planning/phases/06-launch/06-RESEARCH.md (Pitfall 5 — 0.1.0 may already exist; Open Q #2, #3)
    - .github/workflows/publish.yml (uses secrets.NPM_TOKEN + OIDC id-token:write)
  </read_first>
  <decision>How to proceed with the npm publish given current registry + secret state.</decision>
  <action>
    Run `npm view @voke-sh/voke versions 2>/dev/null || echo "not yet published"` and `gh secret list 2>/dev/null | grep -i NPM_TOKEN || echo "NPM_TOKEN not set"`. Report both results, then choose: publish-fresh (not yet published + token set), verify-only (0.1.0 already live and working), or bump-patch (0.1.0 exists but broken → 0.1.1). Record the final version string for downstream tasks. npm versions are immutable; the same version cannot be republished.
  </action>
  <verify>npm view @voke-sh/voke versions result reported AND NPM_TOKEN presence reported AND a branch selected</verify>
  <done>Publish state and NPM_TOKEN state reported; branch chosen with final version string recorded.</done>
  <context>
    Run these checks first and report results, then choose the branch:

    1. Is the version already published?
       `npm view @voke-sh/voke versions 2>/dev/null || echo "not yet published"`

    2. Is the NPM_TOKEN secret set on the repo?
       `gh secret list 2>/dev/null | grep -i NPM_TOKEN || echo "NPM_TOKEN not visible / not set"`
       (Secret VALUES are never visible; only presence. If gh cannot confirm, the user must verify in GitHub Settings -> Secrets -> Actions.)

    npm versions are immutable — the same version cannot be republished (403).
  </context>
  <options>
    <option id="publish-fresh">
      <name>Publish fresh 0.1.0 (RECOMMENDED if not yet published AND NPM_TOKEN is set)</name>
      <pros>Clean first release matching MTQS v0.1; full OIDC provenance via publish.yml</pros>
      <cons>Requires NPM_TOKEN confirmed set, else publish fails late</cons>
    </option>
    <option id="verify-only">
      <name>Skip publish, verify-only (if 0.1.0 already live and working)</name>
      <pros>No re-publish needed; go straight to npx verification</pros>
      <cons>Must confirm the live 0.1.0 actually works end-to-end</cons>
    </option>
    <option id="bump-patch">
      <name>Bump to 0.1.1 (if 0.1.0 exists but is broken)</name>
      <pros>Recovers from a broken/partial earlier publish</pros>
      <cons>Requires editing package.json version + the blog/CTA must then reference 0.1.1; surface the version drift</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `npm view @voke-sh/voke versions` result is reported (published versions list OR "not yet published")
    - NPM_TOKEN presence is reported (set / not set / user-must-verify)
    - A branch is chosen: publish-fresh | verify-only | bump-patch
    - If bump-patch chosen, the new version string is recorded for Plan 03 CTA references
  </acceptance_criteria>
  <resume-signal>Select: publish-fresh, verify-only, or bump-patch (and state the final version string)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the v0.1.0 GitHub Release to trigger publish.yml (skip if verify-only)</name>
  <files>(no repo file writes — creates a GitHub Release; publish.yml moves the v0 tag)</files>
  <read_first>
    - .github/workflows/publish.yml (release-triggered; confirms what the release event runs)
    - .planning/phases/06-launch/06-RESEARCH.md (§"npm release + verification ritual")
  </read_first>
  <action>
    ONLY if Task 1 chose publish-fresh or bump-patch. If verify-only, SKIP to Task 3.

    If bump-patch: first edit packages/linter/package.json "version" to the new value, commit it, then use that version below.

    Create the GitHub Release (this is the trigger for publish.yml):
    ```
    gh release create v0.1.0 \
      --title "voke 0.1.0 (MTQS v0.1)" \
      --notes "First public release of voke — the deterministic MCP Tool Quality linter. Implements MTQS v0.1 (22 rules). Try it: npx -y @voke-sh/voke@0.1.0 lint <server>"
    ```
    (Replace v0.1.0 with the bumped tag if bump-patch.)

    Then WATCH the publish workflow to completion:
    ```
    gh run watch $(gh run list --workflow=publish.yml --limit 1 --json databaseId -q '.[0].databaseId')
    ```
    If the run fails on the publish step with a token/auth error, that is the NPM_TOKEN gap from Task 1 — STOP and surface it; do not proceed to verification.
  </action>
  <verify>
    <automated>gh run list --workflow=publish.yml --limit 1 --json conclusion -q '.[0].conclusion' | grep -qx success && echo PUBLISH_OK</automated>
  </verify>
  <acceptance_criteria>
    - A GitHub Release for the chosen tag exists (`gh release view <tag>` exits 0)
    - The latest publish.yml run concluded "success" (the grep above prints PUBLISH_OK)
    - (Skipped entirely and noted in SUMMARY if Task 1 chose verify-only)
  </acceptance_criteria>
  <done>Release created and publish.yml completed successfully (or step skipped under verify-only).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the published @voke-sh/voke@0.1.0 end-to-end via npx</name>
  <what-built>
    @voke-sh/voke@0.1.0 (or the bumped version) published to the public npm registry via publish.yml with OIDC provenance, and the v0 moving tag updated.
  </what-built>
  <action>
    Verify the published artifact end-to-end (D-11 gate). Run `npx -y @voke-sh/voke@<version> --version` (expect tool version + MTQS version) and `npx -y @voke-sh/voke@<version> lint https://mcp.apideck.dev/mcp --ci` (expect score + A-F tier, exit 0). Optionally lint DeepWiki too. Confirm the live Apideck score matches the Plan 01 captured fixture. Do NOT approve until both pass; Plan 03 depends on this approval. Record the verified version + verbatim score/tier in the SUMMARY.
  </action>
  <verify>npx --version prints version+MTQS AND npx lint apideck --ci exits 0 with a score+tier</verify>
  <done>Published package verified working end-to-end via npx; version + live score/tier recorded for the blog.</done>
  <read_first>
    - .planning/phases/06-launch/06-RESEARCH.md (§"npm release + verification ritual", D-11 strict ordering)
    - README.md (the CTA — confirm the published command matches what ships)
  </read_first>
  <how-to-verify>
    Run these from a clean shell (use the bumped version if Task 1 chose bump-patch). The npx cache may lag a minute or two after publish; retry if 404.

    1. Version + MTQS surface:
       `npx -y @voke-sh/voke@0.1.0 --version`
       Expect: a line printing the voke version AND the MTQS version (e.g. "voke 0.1.0 (MTQS v0.1)").

    2. Live Apideck lint end-to-end (the literal DoD command, PUB-04):
       `npx -y @voke-sh/voke@0.1.0 lint https://mcp.apideck.dev/mcp --ci`
       Expect: per-rule findings + per-tool scores + a server score + an A-F tier; process exits 0.

    3. (Optional, second-server live proof) DeepWiki:
       `npx -y @voke-sh/voke@0.1.0 lint https://mcp.deepwiki.com/mcp --ci`
       Expect: a valid score + tier, exit 0.

    Confirm the printed Apideck score+tier matches the captured fixture's score from Plan 01 (same surface → same score; if the live surface drifted, note it per D-03).

    D-11 GATE: do NOT approve until step 1 and step 2 both pass. Plan 03 (blog) depends on this approval.
  </how-to-verify>
  <acceptance_criteria>
    - `npx -y @voke-sh/voke@0.1.0 --version` prints both the tool version and the MTQS version
    - `npx -y @voke-sh/voke@0.1.0 lint https://mcp.apideck.dev/mcp --ci` exits 0 and prints a server score + an A-F tier
    - The live Apideck score+tier matches the Plan 01 captured-fixture score (or drift is documented per D-03)
    - The verified version string + the verbatim score/tier are recorded in the SUMMARY for the blog CTA
  </acceptance_criteria>
  <resume-signal>Type "approved" once both npx commands pass, or describe the failure</resume-signal>
</task>

</tasks>

<verification>
- `npm view @voke-sh/voke version` returns the released version (0.1.0 or bumped)
- `npx -y @voke-sh/voke@<version> lint https://mcp.apideck.dev/mcp --ci` exits 0 with a score + tier
- v0 moving tag updated by publish.yml (`git ls-remote --tags origin v0` resolves)
- D-11 satisfied: this plan fully verified BEFORE Plan 03 starts
</verification>

<success_criteria>
- @voke-sh/voke@<version> live on npm with provenance
- npx --version and npx lint both verified working end-to-end against the live Apideck server
- Verified version + verbatim live score/tier handed to Plan 03 for the blog CTA + numbers
</success_criteria>

<output>
After completion, create `.planning/phases/06-launch/06-02-SUMMARY.md` — MUST record the final published version string and the verbatim npx lint score+tier (Plan 03's CTA and proof numbers depend on these).
</output>
