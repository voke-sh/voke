---
phase: 05-ci-publication
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/.vitepress/config.ts
  - docs/index.md
  - docs/spec/index.md
  - docs/public/CNAME
  - .github/workflows/docs.yml
  - .gitignore
autonomous: false
requirements: [PUB-01]
user_setup:
  - service: github-pages
    why: "Deploy VitePress site to GitHub Pages with voke.sh custom domain (D-08)"
    dashboard_config:
      - task: "Enable GitHub Pages with source = GitHub Actions"
        location: "GitHub repo -> Settings -> Pages -> Build and deployment -> Source: GitHub Actions"
      - task: "DNS for voke.sh (apex -> GitHub Pages A records, or www CNAME -> voke-sh.github.io); interim site is voke-sh.github.io"
        location: "Domain registrar DNS panel"
must_haves:
  truths:
    - "npm run docs:build builds the VitePress site without error and emits docs/.vitepress/dist"
    - "The built dist contains the v0.1 spec rendered under /spec/v0.1/ (copied from spec/, the source of truth, which is never deleted)"
    - "docs/public/CNAME contains exactly voke.sh so the custom domain survives re-deploys (Pitfall 8)"
    - "VitePress base is '/' (custom-domain root, not a repo subpath) so assets resolve (Pitfall 7)"
    - "Spec versions are independently citable: /spec/ lists v0.1 with a permanent link (D-10)"
  artifacts:
    - path: "docs/.vitepress/config.ts"
      provides: "Minimal VitePress config (title MTQS, base '/', spec sidebar)"
      contains: "defineConfig"
    - path: "docs/public/CNAME"
      provides: "Custom domain marker"
      contains: "voke.sh"
    - path: ".github/workflows/docs.yml"
      provides: "Build + deploy to GitHub Pages on spec/docs changes"
      contains: "actions/deploy-pages"
    - path: "docs/spec/index.md"
      provides: "Versions landing page with permanent v0.1 link"
      contains: "v0.1"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "docs/spec/v0.1/MTQS-v0.1.md"
      via: "cp spec/MTQS-v0.1.md docs/spec/v0.1/ before docs:build"
      pattern: "cp spec/MTQS-v0.1.md"
    - from: "docs/.vitepress/config.ts base"
      to: "voke.sh root"
      via: "base: '/'"
      pattern: "base: '/'"
    - from: "docs/public/CNAME"
      to: "GitHub Pages custom domain"
      via: "VitePress copies public/ verbatim to dist"
      pattern: "voke.sh"
---

<objective>
Publish the MTQS spec as a versioned static docs site (PUB-01) built with VitePress (D-08), deployed to GitHub Pages with the voke.sh custom domain. The spec lives in the same repo (D-09); `spec/MTQS-v0.1.md` stays the immutable source of truth and is copied into `docs/spec/v0.1/` at build time (never deleted). Versions are kept and independently citable (D-10).

Purpose: A public, citable, PR-accepting home for the spec — the "product" half of voke.
Output: a `docs/` VitePress site, a deploy workflow, a CNAME, and `docs:build` wired into root scripts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-publication/05-CONTEXT.md
@.planning/phases/05-ci-publication/05-RESEARCH.md

<interfaces>
<!-- CURRENT state — extracted. -->

Root package.json (CURRENT — add docs scripts + vitepress devDep):
```json
{
  "name": "voke", "type": "module", "private": true,
  "workspaces": ["packages/*"],
  "scripts": { "test": "vitest run", "test:spec": "vitest run tests/spec/", "typecheck": "tsc --build" },
  "devDependencies": { "@types/node": "^22.0.0", "tsup": "^8.5.1", "typescript": "^5.6.0", "vitest": "4.1.8" }
}
```

Source-of-truth spec files (DO NOT MOVE OR DELETE — copy into docs/ for rendering only):
- spec/MTQS-v0.1.md  (the spec; ~58KB)
- spec/SCOPE.md
- spec/mtqs-v0.1.yaml (machine-readable registry)

VitePress facts (RESEARCH Pattern 6, verified vitepress 1.6.4):
- Install: `npm install --save-dev vitepress@1.6.4` (root workspace devDep).
- base MUST be '/' for a custom domain (NOT '/voke/') — Pitfall 7.
- CNAME goes in docs/public/CNAME (copied verbatim to dist) — Pitfall 8; do not rely on the GitHub UI alone.
- Deploy workflow uses actions/upload-pages-artifact@v3 + actions/deploy-pages@v4 with permissions pages:write + id-token:write.
- spec files are COPIED into docs/spec/v0.1/ in the workflow before docs:build (do NOT symlink; copy keeps spec/ immutable as source of truth).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold VitePress site (config, landing, versioned spec index, CNAME) + root scripts</name>
  <files>package.json, docs/.vitepress/config.ts, docs/index.md, docs/spec/index.md, docs/public/CNAME, .gitignore</files>
  <read_first>
    - package.json (root — add docs:dev/docs:build/docs:preview scripts + vitepress devDependency)
    - spec/MTQS-v0.1.md (the spec the site renders; confirm the top-level title so sidebar links match)
    - spec/SCOPE.md (also rendered under v0.1)
    - .gitignore (must add docs/.vitepress/dist and docs/spec/v0.1 copies so build artifacts/copies are not committed)
  </read_first>
  <action>
    1. Install VitePress as a root devDependency: `npm install --save-dev vitepress@1.6.4`.
    2. Add root package.json scripts: `"docs:dev": "vitepress dev docs"`, `"docs:build": "vitepress build docs"`, `"docs:preview": "vitepress preview docs"`.
    3. Create docs/.vitepress/config.ts using `defineConfig` (RESEARCH Pattern 6 verbatim):
       - title 'MTQS', description 'MCP Tool Quality Specification', `base: '/'`
       - themeConfig.nav: Spec -> '/spec/', GitHub -> 'https://github.com/voke-sh/voke'
       - themeConfig.sidebar for '/spec/': a 'Versions' link to '/spec/' and a 'v0.1' group with items linking '/spec/v0.1/MTQS-v0.1' and '/spec/v0.1/SCOPE'
    4. Create docs/index.md — VitePress home page: title MTQS, one-line tagline ("An open, versioned, deterministic ruleset for MCP tool quality"), a link to the spec and to GitHub.
    5. Create docs/spec/index.md — the Versions landing page: a heading "MTQS Versions" and a permanent list linking v0.1 (`/spec/v0.1/MTQS-v0.1`). State that old versions remain immutable and independently citable (D-10).
    6. Create docs/public/CNAME containing exactly the single line `voke.sh` (no trailing content).
    7. Update .gitignore: add `docs/.vitepress/dist/` and `docs/spec/v0.1/` (the v0.1 copies are generated at build time by the workflow, not committed — the source of truth is spec/).
    8. Locally verify the build path: create the copy dir and copy the spec so `docs:build` resolves the sidebar links, then run `npm run docs:build`.
  </action>
  <verify>
    <automated>mkdir -p docs/spec/v0.1 && cp spec/MTQS-v0.1.md docs/spec/v0.1/ && cp spec/SCOPE.md docs/spec/v0.1/ && npm run docs:build && test -d docs/.vitepress/dist && test -f docs/.vitepress/dist/CNAME</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "defineConfig" docs/.vitepress/config.ts` and `grep -q "base: '/'" docs/.vitepress/config.ts`
    - `grep -q "voke.sh" docs/public/CNAME` and the file is a single line
    - `grep -q "docs:build" package.json` and `grep -q "vitepress" package.json` (devDependency present)
    - `grep -q "v0.1" docs/spec/index.md`
    - `.gitignore` contains `docs/.vitepress/dist/`
    - `npm run docs:build` exits 0
    - `test -d docs/.vitepress/dist` and `test -f docs/.vitepress/dist/CNAME` (CNAME copied verbatim from public/)
    - dist contains the rendered spec: `test -f docs/.vitepress/dist/spec/v0.1/MTQS-v0.1.html`
  </acceptance_criteria>
  <done>VitePress site builds locally; CNAME present in dist; v0.1 spec rendered; root has docs:* scripts and vitepress devDep.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Pages deploy workflow (copy spec -> build -> deploy)</name>
  <files>.github/workflows/docs.yml</files>
  <read_first>
    - .planning/phases/05-ci-publication/05-RESEARCH.md (Pattern 6 deploy workflow YAML — copy verbatim, adjust paths)
    - docs/.vitepress/config.ts (the build target; confirm output dir docs/.vitepress/dist)
    - spec/MTQS-v0.1.md and spec/SCOPE.md (the files the workflow copies into docs/spec/v0.1/)
  </read_first>
  <action>
    Create .github/workflows/docs.yml (RESEARCH Pattern 6 verbatim):
    - triggers: `push: branches: [main], paths: ['spec/**', 'docs/**']` plus `workflow_dispatch`
    - permissions: `contents: read`, `pages: write`, `id-token: write`
    - concurrency: `group: pages`, `cancel-in-progress: false`
    - build job (ubuntu-latest): checkout@v4 -> setup-node@v4 (node '22', cache npm) -> `npm ci` -> a "Copy spec files into docs" step running `mkdir -p docs/spec/v0.1 && cp spec/MTQS-v0.1.md docs/spec/v0.1/ && cp spec/SCOPE.md docs/spec/v0.1/` -> `npm run docs:build` -> `actions/upload-pages-artifact@v3` with `path: docs/.vitepress/dist`
    - deploy job: `needs: build`, `environment: { name: github-pages, url: ${{ steps.deployment.outputs.page_url }} }`, step `actions/deploy-pages@v4` with `id: deployment`
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/docs.yml','utf8'); for(const s of ['cp spec/MTQS-v0.1.md docs/spec/v0.1/','npm run docs:build','actions/upload-pages-artifact@v3','actions/deploy-pages@v4','pages: write','id-token: write','path: docs/.vitepress/dist']){ if(!y.includes(s)) throw new Error('docs.yml missing: '+s);} console.log('docs.yml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/docs.yml`
    - `grep -q "cp spec/MTQS-v0.1.md docs/spec/v0.1/" .github/workflows/docs.yml`
    - `grep -q "actions/upload-pages-artifact@v3" .github/workflows/docs.yml`
    - `grep -q "actions/deploy-pages@v4" .github/workflows/docs.yml`
    - `grep -q "pages: write" .github/workflows/docs.yml` and `grep -q "id-token: write" .github/workflows/docs.yml`
    - `grep -q "path: docs/.vitepress/dist" .github/workflows/docs.yml`
    - workflow triggers on `paths: ['spec/**', 'docs/**']`
  </acceptance_criteria>
  <done>docs.yml copies the immutable spec into docs/spec/v0.1/, builds VitePress, and deploys to GitHub Pages with the correct permissions.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Enable GitHub Pages + voke.sh DNS (human-only)</name>
  <action>Human-only step: in GitHub repo Settings -> Pages set Source to GitHub Actions, trigger docs.yml, and (when voke.sh is owned) point DNS at GitHub Pages. See how-to-verify for exact steps. Interim github.io URL is acceptable until DNS is ready.</action>
  <what-built>VitePress site + deploy workflow are committed. Two environment steps need a human: enabling GitHub Pages (Actions source) and pointing the voke.sh DNS.</what-built>
  <how-to-verify>
    1. Enable Pages: repo -> Settings -> Pages -> Build and deployment -> Source: "GitHub Actions".
    2. Trigger the workflow: push to main touching spec/ or docs/, or run docs.yml via workflow_dispatch. Confirm the run is green and the deploy job prints a page_url.
    3. Interim verification (no DNS yet): the site is reachable at https://voke-sh.github.io/spec/ and v0.1 renders.
    4. (When voke.sh DNS is ready) Add DNS records at the registrar: apex `voke.sh` -> GitHub Pages A records (185.199.108–111.153), or use a www CNAME -> voke-sh.github.io. Then confirm https://voke.sh/spec/ resolves and v0.1 renders. If voke.sh is not yet owned, the CNAME file can stay as-is; the interim github.io URL is the fallback (RESEARCH Open Question 2).
  </how-to-verify>
  <resume-signal>Type "approved" once the docs workflow deploys green (github.io interim acceptable) or describe a blocker (Pages not enabling, DNS pending).</resume-signal>
</task>

</tasks>

<verification>
- `npm run docs:build` exits 0 with CNAME and v0.1 spec in dist
- docs.yml present with copy-spec step, upload-pages-artifact@v3, deploy-pages@v4, correct permissions
- Human checkpoint: Pages enabled (Actions source); site deploys (github.io interim acceptable); voke.sh DNS pending-or-done
</verification>

<success_criteria>
- PUB-01 satisfied: MTQS v0.1 published and versioned at voke.sh/spec (or github.io interim), independently citable, in a public repo accepting PRs
- spec/MTQS-v0.1.md remains the immutable source of truth; docs copies are build-time only
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-publication/05-03-SUMMARY.md`
</output>
