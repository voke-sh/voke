---
phase: 05-ci-publication
plan: 04
type: execute
wave: 2
depends_on: ["05-02", "05-03"]
files_modified:
  - LICENSE
  - README.md
  - CONTRIBUTING.md
  - .github/pull_request_template/rule_pr.md
files_modified_note: "Wave 2 — depends on 05-02 (Action + @voke-sh/voke name) and 05-03 (voke.sh/spec URL) for accurate references. No file overlap with prior plans."
autonomous: false
requirements: [CI-02, PUB-02]
must_haves:
  truths:
    - "README leads with a copy-paste-runnable GitHub Action workflow snippet (uses: voke-sh/voke@v0 + min-score) that works in a new repo with zero modification (CI-02 #3, D-11)"
    - "README also shows the local one-liner npx @voke-sh/voke lint <url> (scoped name, not voke)"
    - "LICENSE is the verbatim Apache-2.0 text (D-12)"
    - "CONTRIBUTING.md links SCOPE.md and the rule PR template; README links CONTRIBUTING.md and the rule PR template"
    - "The rule PR template makes a primary-source citation (never Glama) mandatory, plus positive+negative fixtures and determinism preservation (D-13, SCOPE.md §4)"
  artifacts:
    - path: "LICENSE"
      provides: "Apache-2.0 verbatim"
      contains: "Apache License"
    - path: "README.md"
      provides: "Action-first quickstart + local one-liner + links to CONTRIBUTING and spec"
      contains: "uses: voke-sh/voke@v0"
    - path: "CONTRIBUTING.md"
      provides: "Rule-PR rigor, links SCOPE.md + rule PR template"
      contains: "SCOPE.md"
    - path: ".github/pull_request_template/rule_pr.md"
      provides: "Rule PR template enforcing primary-source citation (never Glama)"
      contains: "primary-source"
  key_links:
    - from: "README.md"
      to: "GitHub Action (action.yml from Plan 02)"
      via: "uses: voke-sh/voke@v0 with min-score"
      pattern: "uses: voke-sh/voke@v0"
    - from: "CONTRIBUTING.md"
      to: "spec/SCOPE.md §4 primary-source rule"
      via: "markdown link"
      pattern: "SCOPE.md"
    - from: "README.md"
      to: "voke.sh/spec (Plan 03 site)"
      via: "spec link"
      pattern: "voke.sh/spec"
---

<objective>
Make the repo ready to go public (CI-02, PUB-02): an Action-first README that is copy-paste runnable, the Apache-2.0 LICENSE, a CONTRIBUTING.md that enforces rule-PR rigor, and a rule PR template that makes primary-source citation (never Glama) impossible to skip.

Purpose: CI-02 criterion #3 (paste the README snippet into a fresh repo -> working lint job, no modification) and PUB-02 (contribution docs exist and are linked) before the repo goes public.
Output: LICENSE, README.md, CONTRIBUTING.md, rule PR template.

CRITICAL references this plan must get exactly right (from Plans 02/03):
- Action reference: `uses: voke-sh/voke@v0` (major moving tag; mention full-SHA pinning as the security option, D-07).
- npm name: `@voke-sh/voke` everywhere (the unscoped `voke` is taken) — `npx @voke-sh/voke lint ...`.
- Spec URL: voke.sh/spec (github.io interim acceptable).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-publication/05-CONTEXT.md
@.planning/phases/05-ci-publication/05-RESEARCH.md

<interfaces>
<!-- Inputs this plan references from earlier plans. -->

From Plan 02 (action.yml — the contract the README snippet must match):
```yaml
# uses: voke-sh/voke@v0
# inputs: target (required), min-score (default '0'), format (default 'human'), args (default ''), version (default 'latest')
```

From Plan 02 (published package): name `@voke-sh/voke`, bin `voke`, `--version` prints `voke 0.1.0 (MTQS v0.1)`.
CLI flags (from program.ts): `lint <target>`, `--min-score`, `--output human|json`, `-H/--header`, `--timeout`, `--ci`, `--env KEY=VAL`, stdio via `lint -- <cmd>`.

From Plan 03 (spec site): published at voke.sh/spec (interim voke-sh.github.io/spec).

Source-of-truth governance file the contribution docs must link:
- spec/SCOPE.md §4 — "primary source citation required, never Glama" (the originality/determinism wedge guard).

The exact zero-modification README Action snippet (RESEARCH Code Examples — use verbatim):
```yaml
# .github/workflows/mcp-lint.yml
name: MCP Tool Quality Check
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: voke-sh/voke@v0
        with:
          target: 'https://your-mcp-server.example.com/mcp'
          min-score: '70'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apache-2.0 LICENSE + Action-first README (copy-paste runnable)</name>
  <files>LICENSE, README.md</files>
  <read_first>
    - .planning/phases/05-ci-publication/05-RESEARCH.md (the verbatim README Action snippet under Code Examples; Apache-2.0 source URL under Don't Hand-Roll)
    - .planning/phases/05-ci-publication/05-02-PLAN.md (action.yml input names the README snippet must match exactly)
    - .planning/phases/05-ci-publication/05-03-PLAN.md (spec site URL voke.sh/spec)
    - CLAUDE.md (the Voke positioning + core value line for the README intro)
  </read_first>
  <action>
    1. Create LICENSE with the verbatim Apache License 2.0 text (D-12). Use the canonical text from https://www.apache.org/licenses/LICENSE-2.0.txt — full text including the "Apache License / Version 2.0, January 2004" header and the appendix. Fill the copyright line as `Copyright 2026 voke-sh`.
    2. Create README.md structured as:
       - H1 "Voke" + one-line positioning: deterministic MCP tool quality linter, MTQS v0.1 reference implementation.
       - A short "What" paragraph: `voke lint <server>` produces deterministic per-rule findings + a stable score against the published MTQS ruleset (same input -> same output).
       - **Quickstart leads with the GitHub Action** (D-11, CI-02 #3): the EXACT verbatim snippet from the interfaces block (`.github/workflows/mcp-lint.yml`, `uses: voke-sh/voke@v0`, target + min-score). State it is copy-paste runnable into a new repo with zero modification. Add a one-line note: pin to `@v0` for auto patch/minor updates, or pin to a full commit SHA for the security-conscious option (D-07).
       - **Local usage** below the Action: `npx @voke-sh/voke lint https://your-mcp-server.example.com/mcp` (scoped name — NOT `npx voke`), plus stdio (`npx @voke-sh/voke lint -- node server.js`) and snapshot-file modes, `--min-score`, `--output json`, `--header`, `--env`.
       - **Exit codes** brief table (0 pass / 1 below min-score / 8 stdio launch / 9 stdio teardown ... reference the others).
       - **Spec** section linking voke.sh/spec (interim voke-sh.github.io/spec) and noting versions are immutable + citable.
       - **Contributing** section linking CONTRIBUTING.md and the rule PR template (`.github/pull_request_template/rule_pr.md`).
       - **License** line: Apache-2.0.
       Do NOT use the unscoped `npx voke` anywhere. Do NOT use em dashes (project editorial convention — use hyphens/commas).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('README.md','utf8'); const l=fs.readFileSync('LICENSE','utf8'); if(!l.includes('Apache License')||!l.includes('Version 2.0'))throw new Error('LICENSE not Apache-2.0'); for(const s of ['uses: voke-sh/voke@v0','min-score','npx @voke-sh/voke','voke.sh/spec','CONTRIBUTING.md','rule_pr.md']){ if(!r.includes(s)) throw new Error('README missing: '+s);} if(/npx voke[^-]/.test(r)||r.includes('npx voke ')) throw new Error('README uses unscoped npx voke'); if(r.includes('—')) throw new Error('README contains em dash'); console.log('readme+license ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSE` and `grep -q "Apache License" LICENSE` and `grep -q "Version 2.0" LICENSE`
    - `grep -q "uses: voke-sh/voke@v0" README.md` (Action snippet matches Plan 02 contract)
    - `grep -q "min-score: '70'" README.md` (the verbatim zero-mod snippet)
    - `grep -q "npx @voke-sh/voke" README.md` (scoped name)
    - README does NOT contain the unscoped string `npx voke ` (scoped only)
    - `grep -q "voke.sh/spec" README.md`
    - `grep -q "CONTRIBUTING.md" README.md` and `grep -q "rule_pr.md" README.md`
    - README contains no em dash character (project editorial rule)
  </acceptance_criteria>
  <done>Apache-2.0 LICENSE present; README leads with the zero-modification Action snippet, shows scoped local usage, and links the spec + contribution docs.</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING.md + rule PR template enforcing primary-source citation</name>
  <files>CONTRIBUTING.md, .github/pull_request_template/rule_pr.md</files>
  <read_first>
    - spec/SCOPE.md (read §4 — the "primary source citation required, never Glama" rule the docs must enforce and link)
    - README.md (the file just written — CONTRIBUTING/template must be link-consistent with it)
    - .planning/phases/05-ci-publication/05-CONTEXT.md (D-13 rule-PR rigor: citation + positive/negative fixtures + determinism)
  </read_first>
  <action>
    1. Create CONTRIBUTING.md (D-13) with sections:
       - Intro: how to contribute to voke (linter) and MTQS (spec).
       - **Rule contributions** (the rigor gate): every new/changed rule MUST include (a) a PRIMARY-SOURCE citation — Anthropic / MCP spec / JSON Schema / academic — and NEVER Glama (link to spec/SCOPE.md §4 explicitly); (b) a positive fixture (known-good tool that passes) AND a negative fixture (known-bad tool that fires the rule); (c) a registry entry in spec/mtqs-v0.1.yaml (the build check fails on a rule-in-code without a registry entry); (d) preserved determinism (no IO in rule bodies, no LLM, byte-identical x3).
       - **Dev setup**: `npm ci`, `npm test`, `npm --workspace @voke-sh/voke run build`.
       - **Determinism contract**: link to the determinism requirement; rules are pure `(context) => Finding[]`.
       - A line: "All rule PRs use the rule PR template (.github/pull_request_template/rule_pr.md)."
       - Link to spec/SCOPE.md and to the rule PR template.
       Use hyphens, not em dashes.
    2. Create .github/pull_request_template/rule_pr.md — a checklist-style template the contributor fills:
       - `## Rule` (id + dimension + severity)
       - `## Primary-source citation` (REQUIRED): a checkbox `- [ ] I cite a primary source (Anthropic / MCP spec / JSON Schema / academic). I am NOT citing Glama (see SCOPE.md §4).` plus a free-text field for the citation URL/reference.
       - `## Fixtures`: `- [ ] Positive fixture added (passes)` / `- [ ] Negative fixture added (fires the rule)`.
       - `## Registry`: `- [ ] Rule has an entry in spec/mtqs-v0.1.yaml (id, severity, dimension)`.
       - `## Determinism`: `- [ ] No IO / no network / no LLM in the rule body` / `- [ ] Output is byte-identical across runs`.
       - A closing note linking SCOPE.md §4.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('CONTRIBUTING.md','utf8'); const t=fs.readFileSync('.github/pull_request_template/rule_pr.md','utf8'); for(const s of ['SCOPE.md','rule_pr.md','npm test','@voke-sh/voke']){ if(!c.includes(s)) throw new Error('CONTRIBUTING missing: '+s);} for(const s of ['primary-source','Glama','Positive fixture','Negative fixture','mtqs-v0.1.yaml','Determinism']){ if(!t.toLowerCase().includes(s.toLowerCase())) throw new Error('rule_pr missing: '+s);} if(c.includes('—')||t.includes('—')) throw new Error('em dash present'); console.log('contributing+template ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f CONTRIBUTING.md` and `test -f .github/pull_request_template/rule_pr.md`
    - CONTRIBUTING.md links `SCOPE.md` and references the rule PR template `rule_pr.md`
    - CONTRIBUTING.md mentions positive + negative fixtures and determinism preservation
    - rule_pr.md contains a mandatory primary-source citation checkbox AND the explicit "NOT ... Glama" guard
    - rule_pr.md contains positive-fixture, negative-fixture, registry (mtqs-v0.1.yaml), and determinism checkboxes
    - Neither file contains an em dash character
  </acceptance_criteria>
  <done>CONTRIBUTING.md enforces rule-PR rigor and links SCOPE.md + the template; the rule PR template makes primary-source citation (never Glama) + fixtures + determinism mandatory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify README quickstart is copy-paste runnable + links resolve</name>
  <action>Human verification of the public-facing surface: read README top-to-bottom, confirm the Action snippet is the first quickstart and is zero-modification, confirm all internal links resolve, and confirm scoped @voke-sh/voke is used everywhere with no em dashes. See how-to-verify for the CI-02 #3 paste test.</action>
  <what-built>The public-facing repo surface: Apache-2.0 LICENSE, Action-first README, CONTRIBUTING.md, and the rule PR template. The README Action snippet is the CI-02 #3 artifact (copy-paste runnable).</what-built>
  <how-to-verify>
    1. Read README.md top-to-bottom: confirm the Action snippet is the FIRST quickstart and reads as zero-modification (only `target` needs the user's real server URL).
    2. CI-02 #3 real test (the actual acceptance): paste the README `.github/workflows/mcp-lint.yml` snippet into a fresh test repo (or a branch), set `target` to a reachable MCP server, push, and confirm the lint job runs and gates on `min-score`. (Requires Plan 02's package to be published + voke-sh org live — coordinate with the Phase 6 release if not yet published; until then verify the snippet is syntactically a valid workflow.)
    3. Confirm all internal links resolve: README -> CONTRIBUTING.md -> SCOPE.md -> rule_pr.md, and README -> voke.sh/spec (or github.io interim).
    4. Confirm no em dashes and the scoped name `@voke-sh/voke` is used everywhere (no `npx voke`).
  </how-to-verify>
  <resume-signal>Type "approved" if the README quickstart is copy-paste runnable and links resolve, or describe what to fix.</resume-signal>
</task>

</tasks>

<verification>
- LICENSE is verbatim Apache-2.0
- README leads with the zero-modification Action snippet (uses: voke-sh/voke@v0 + min-score) and uses scoped npx @voke-sh/voke everywhere
- CONTRIBUTING.md + rule PR template exist, are linked from README, and enforce primary-source citation (never Glama) + fixtures + determinism
- No em dashes in any new doc
</verification>

<success_criteria>
- CI-02 satisfied: README doubles as the demo; its Action snippet is copy-paste runnable into a new repo with no modification
- PUB-02 satisfied: CONTRIBUTING.md + rule PR template exist before the repo goes public; both linked from README; SCOPE.md linked from CONTRIBUTING.md
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-publication/05-04-SUMMARY.md`
</output>
