---
phase: 05-ci-publication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/linter/package.json
  - packages/linter/src/version.ts
  - action.yml
  - .github/workflows/publish.yml
  - .github/workflows/ci.yml
autonomous: false
requirements: [CI-01]
user_setup:
  - service: npm
    why: "Publish @voke-sh/voke so the Action's npx and the README one-liner resolve (D-05)"
    env_vars:
      - name: NPM_TOKEN
        source: "npmjs.com → Access Tokens → Generate (Automation/Granular, publish scope @voke-sh) → add as GitHub repo secret NPM_TOKEN"
    dashboard_config:
      - task: "Create npm org 'voke-sh' (scope owner)"
        location: "https://www.npmjs.com/org/create"
  - service: github
    why: "OIDC provenance for npm publish"
    dashboard_config:
      - task: "Repo must allow id-token: write (default for Actions); no extra config beyond workflow permissions block"
        location: "GitHub repo → Settings → Actions → General"
must_haves:
  truths:
    - "packages/linter/package.json publishes as @voke-sh/voke (NOT voke — that name is taken), is public, and ships only dist with bin voke"
    - "action.yml at repo root is a composite action that sets up Node 22 and runs npx @voke-sh/voke@<version> lint with min-score/target/format/args inputs"
    - "Every run step in action.yml declares shell: bash (composite requirement)"
    - "publish.yml publishes @voke-sh/voke on GitHub release with --provenance and id-token: write"
    - "VOKE_VERSION matches package.json version (0.1.0) so --version reports the published version"
  artifacts:
    - path: "action.yml"
      provides: "Composite GitHub Action wrapping npx @voke-sh/voke lint"
      contains: "using: 'composite'"
    - path: ".github/workflows/publish.yml"
      provides: "npm publish on release with provenance"
      contains: "--provenance"
    - path: "packages/linter/package.json"
      provides: "publishable @voke-sh/voke package metadata"
      contains: "@voke-sh/voke"
  key_links:
    - from: "action.yml"
      to: "npm package @voke-sh/voke"
      via: "npx @voke-sh/voke@${{ inputs.version }} lint"
      pattern: "npx @voke-sh/voke"
    - from: "action.yml min-score input"
      to: "CLI --min-score gate (exit 1 below threshold)"
      via: "--min-score ${{ inputs.min-score }}"
      pattern: "--min-score"
    - from: ".github/workflows/publish.yml"
      to: "@voke-sh/voke on npm"
      via: "npm publish --workspace ... --provenance --access public"
      pattern: "npm publish"
---

<objective>
Make voke installable and CI-usable (CI-01): rename the published package to `@voke-sh/voke` (the unscoped `voke` is already taken on npm), wire npm publish-on-release with provenance, and ship a root composite `action.yml` that runs `npx @voke-sh/voke@<ver> lint` with a `min-score` gate.

Purpose: A one-line `uses: voke-sh/voke@v0` in any repo's workflow runs the linter in CI and fails the build below a score threshold.
Output: publishable package.json, `action.yml`, `publish.yml`, a `ci.yml` test workflow, and VOKE_VERSION bumped to 0.1.0.

CRITICAL: the npm name `voke` is TAKEN (existing v1.0.2). Publish as `@voke-sh/voke` (confirmed available). Every `npx`, `name`, and `--workspace` reference uses `@voke-sh/voke`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-publication/05-CONTEXT.md
@.planning/phases/05-ci-publication/05-RESEARCH.md

<interfaces>
<!-- CURRENT state extracted from the codebase — extend, don't rewrite. -->

From packages/linter/package.json (CURRENT — must change name + private + add publishConfig):
```json
{
  "name": "@voke/linter",          // → "@voke-sh/voke"
  "version": "0.0.0",              // → "0.1.0"
  "private": true,                 // → false
  "bin": { "voke": "./dist/cli/index.js" },   // keep
  "files": ["dist"],                          // keep
  "dependencies": { "@voke/core": "*", "@modelcontextprotocol/sdk": "~1.29.0", ... }
}
```
NOTE: `@voke/core` is bundled into dist by tsup (noExternal: ['@voke/core']) — the published package has no runtime workspace dependency. Keep `@voke/core` as a devDependency-equivalent only if the build still resolves; the existing tsup config already inlines it, so the `dependencies` entry `"@voke/core": "*"` MUST be removed from the published package or it will 404 on install. Move build-only deps so the published manifest lists only real npm deps (sdk, ajv, ajv-formats, chalk, commander, zod).

From packages/linter/src/version.ts (CURRENT):
```typescript
export const VOKE_VERSION = '0.0.0';   // → '0.1.0' (must match package.json)
export const MTQS_VERSION = '0.1';     // unchanged
export const versionString = () => `voke ${VOKE_VERSION} (MTQS v${MTQS_VERSION})`;
```

Root package.json: `private: true`, workspaces ['packages/*'], scripts: test/typecheck. Node engines >=22.

GitHub Actions facts (RESEARCH Patterns 4 & 5, verified):
- Composite action: `runs.using: 'composite'`; EVERY run step needs `shell: bash` (Pitfall 5).
- Inputs are STRINGS only (no bool/number).
- `npm publish --workspace @voke-sh/voke --provenance --access public` needs `permissions: id-token: write`.
- Root package.json is private — never `npm publish` from root; always `--workspace`.
- For initial v0.1.0 release the moving major tag is `v0` → README recommends `uses: voke-sh/voke@v0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make @voke-sh/voke publishable + bump VOKE_VERSION + publish workflow</name>
  <files>packages/linter/package.json, packages/linter/src/version.ts, .github/workflows/publish.yml, .github/workflows/ci.yml</files>
  <read_first>
    - packages/linter/package.json (current name @voke/linter, private:true — must flip)
    - packages/linter/tsup.config.ts (confirms @voke/core is bundled via noExternal — so it must NOT be a published runtime dependency)
    - packages/linter/src/version.ts (VOKE_VERSION must match package.json version)
    - package.json (root — confirm workspaces + Node 22 engines; never publish from root)
  </read_first>
  <action>
    1. Edit packages/linter/package.json:
       - `"name": "@voke-sh/voke"`
       - `"version": "0.1.0"`
       - `"private": false`
       - add `"publishConfig": { "access": "public", "provenance": true }`
       - add `"description"`, `"license": "Apache-2.0"`, `"repository": { "type": "git", "url": "https://github.com/voke-sh/voke.git", "directory": "packages/linter" }`, `"homepage": "https://voke.sh"`, `"keywords": ["mcp", "linter", "mtqs", "tool-quality", "ci"]`
       - keep `"bin": { "voke": "./dist/cli/index.js" }` and `"files": ["dist"]`
       - REMOVE `"@voke/core": "*"` from `dependencies` (it is bundled into dist by tsup `noExternal`; leaving it makes `npm install @voke-sh/voke` 404 on the private workspace pkg — Pitfall). Keep the real npm runtime deps: @modelcontextprotocol/sdk ~1.29.0, ajv 8.20.0, ajv-formats 3.0.1, chalk 5.6.2, commander 15.0.0, zod 4.4.3.
       - Verify the build still resolves @voke/core at build time: tsup bundles from the workspace source path, which is present in the monorepo during `npm ci` even without a dependencies entry. If tsup needs it declared, move it to `devDependencies` as `"@voke/core": "*"` (dev only, not published as a runtime dep).
    2. Edit packages/linter/src/version.ts: `export const VOKE_VERSION = '0.1.0';` (must equal package.json version so --version reports the published version).
    3. Create .github/workflows/publish.yml (RESEARCH Pattern 5) triggered on `release: types: [published]`:
       - job permissions: `contents: read`, `id-token: write`
       - steps: checkout@v4 → setup-node@v4 (node-version '22', registry-url 'https://registry.npmjs.org') → `npm ci` → `npm --workspace @voke-sh/voke run build` → `npm publish --workspace @voke-sh/voke --provenance --access public` with `env: NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}`
       - add a final step to move the major tag: `git tag -fa v0 -m "Update v0 tag" && git push origin v0 --force` (so `uses: voke-sh/voke@v0` tracks the latest 0.x release).
    4. Create .github/workflows/ci.yml (test gate on push/PR): checkout@v4 → setup-node@v4 (node '22', cache npm) → `npm ci` → `npm test` → `npx tsc --noEmit -p packages/linter` → `npm --workspace @voke-sh/voke run build`. This proves the published artifact builds in CI.
  </action>
  <verify>
    <automated>npm --workspace @voke-sh/voke run build &amp;&amp; node -e "const p=require('./packages/linter/package.json'); const v=require('fs').readFileSync('./packages/linter/src/version.ts','utf8'); if(p.name!=='@voke-sh/voke')throw new Error('name'); if(p.private)throw new Error('private'); if(p.dependencies&&p.dependencies['@voke/core'])throw new Error('@voke/core still a runtime dep'); if(!v.includes(\"'0.1.0'\"))throw new Error('version mismatch'); if(p.version!=='0.1.0')throw new Error('pkg version'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@voke-sh/voke"' packages/linter/package.json` (name field)
    - `grep -q '"version": "0.1.0"' packages/linter/package.json`
    - package.json has `"private": false` and a `publishConfig` with `"access": "public"` and `"provenance": true`
    - `"@voke/core"` is NOT under `dependencies` in packages/linter/package.json
    - `grep -q "VOKE_VERSION = '0.1.0'" packages/linter/src/version.ts`
    - `.github/workflows/publish.yml` contains `--provenance`, `--workspace @voke-sh/voke`, and `id-token: write`
    - `.github/workflows/publish.yml` contains `git push origin v0 --force`
    - `.github/workflows/ci.yml` exists and runs `npm test`
    - `npm --workspace @voke-sh/voke run build` exits 0 (dist/cli/index.js produced)
  </acceptance_criteria>
  <done>Package renamed to @voke-sh/voke, public, version 0.1.0 matching VOKE_VERSION; build still self-contained; publish + ci workflows exist with provenance and major-tag move.</done>
</task>

<task type="auto">
  <name>Task 2: Root composite action.yml wrapping npx @voke-sh/voke lint</name>
  <files>action.yml</files>
  <read_first>
    - packages/linter/src/cli/program.ts (confirm flag names: --min-score, --output, --ci, --header; inputs must map to these)
    - packages/linter/src/cli/run-lint.ts (confirm exit-code 1 below min-score — the build-fail gate)
    - .planning/phases/05-ci-publication/05-RESEARCH.md (Pattern 4 — composite action.yml schema + shell:bash requirement)
  </read_first>
  <action>
    Create action.yml at the REPO ROOT (D-06 — same repo as linter so `uses: voke-sh/voke@v0` resolves). Composite action:
    - `name: 'Voke MCP Linter'`, `description: 'Run voke lint on an MCP server and fail the build below a score threshold'`, `author: 'voke-sh'`
    - inputs (ALL strings — composite constraint):
      - `target` (required: true) — "MCP server URL, snapshot file path, or '--' for stdio mode"
      - `min-score` (required: false, default: '0')
      - `format` (required: false, default: 'human') — human | json
      - `args` (required: false, default: '') — extra verbatim CLI args (e.g. `--header 'Authorization: Bearer $TOKEN'`)
      - `version` (required: false, default: 'latest') — voke version (e.g. 'latest' or '0.1.0')
    - outputs: `score` mapped to `${{ steps.lint.outputs.score }}` (the lint step writes `score=...` to $GITHUB_OUTPUT; if score extraction is non-trivial, document it as best-effort — the build-fail gate is via exit code, not this output)
    - runs.using: 'composite' with steps:
      1. `uses: actions/setup-node@v4` with `node-version: '22'`
      2. `name: Run voke lint`, `id: lint`, `shell: bash` (MANDATORY — Pitfall 5), run: `npx @voke-sh/voke@${{ inputs.version }} lint ${{ inputs.target }} --min-score ${{ inputs.min-score }} --output ${{ inputs.format }} --ci ${{ inputs.args }}`
    The `--ci` flag forces deterministic plain-text. The `--min-score` flag drives the exit-code gate (CLI exits 1 below threshold → step fails → build fails).
  </action>
  <verify>
    <automated>node -e "const yaml=require('fs').readFileSync('action.yml','utf8'); for(const s of ['npx @voke-sh/voke','using:','composite','shell: bash','--min-score','min-score','actions/setup-node@v4',\"node-version: '22'\"]){ if(!yaml.includes(s)) throw new Error('action.yml missing: '+s);} console.log('action.yml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f action.yml` (at repo root)
    - `grep -q "npx @voke-sh/voke@\${{ inputs.version }}" action.yml`
    - `grep -q "using: 'composite'" action.yml`
    - every `run:` step is preceded by a `shell: bash` — `grep -q "shell: bash" action.yml` (and no run step lacks it)
    - `grep -q "node-version: '22'" action.yml` with `actions/setup-node@v4`
    - action.yml defines inputs `target`, `min-score`, `format`, `args`, `version`
    - `grep -q -- "--min-score \${{ inputs.min-score }}" action.yml`
    - action.yml does NOT reference the unscoped `voke@` (i.e. `grep -vq "npx voke@"` — must be scoped)
  </acceptance_criteria>
  <done>Root composite action.yml runs npx @voke-sh/voke lint with Node 22, every run step has shell:bash, and min-score drives the build-fail gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Provision npm org voke-sh + NPM_TOKEN secret (human-only)</name>
  <action>Human-only step (no CLI path from here): create the npm org voke-sh, generate an npm publish token scoped to @voke-sh, and add it as the GitHub repo secret NPM_TOKEN. See how-to-verify for exact steps. Do not run npm publish manually; publish.yml runs it on the first GitHub Release.</action>
  <what-built>Publishing prerequisites that only a human can complete (no CLI/API path Claude can run from here): the npm org and the publish token.</what-built>
  <how-to-verify>
    1. Create the npm org: visit https://www.npmjs.com/org/create and create org `voke-sh` (free tier is fine for public scoped packages).
    2. Generate an npm publish token: npmjs.com → Access Tokens → Generate New Token (Granular Access, publish permission for scope @voke-sh) → copy.
    3. Add it as a GitHub repo secret: repo → Settings → Secrets and variables → Actions → New repository secret, name `NPM_TOKEN`, paste the token.
    4. Confirm `@voke-sh/voke` is still unclaimed: `npm view @voke-sh/voke version` should 404 (E404) until the first release publishes it.
    Note: the actual `npm publish` runs automatically via publish.yml on the first GitHub Release — do NOT publish manually here (Phase 6 cuts the release).
  </how-to-verify>
  <resume-signal>Type "approved" once the voke-sh org exists and NPM_TOKEN is set, or describe any blocker (e.g. org name unavailable).</resume-signal>
</task>

</tasks>

<verification>
- `npm --workspace @voke-sh/voke run build` exits 0 and produces dist/cli/index.js
- `node packages/linter/dist/cli/index.js --version` prints `voke 0.1.0 (MTQS v0.1)`
- action.yml validates as composite (using: composite, all run steps shell: bash, scoped npx)
- publish.yml + ci.yml present; publish.yml uses --provenance + id-token: write + --workspace @voke-sh/voke
- Human checkpoint: voke-sh npm org + NPM_TOKEN secret in place
</verification>

<success_criteria>
- CI-01 satisfied: a composite action runs voke lint in CI and fails the build below min-score
- Package is publishable as @voke-sh/voke (name collision avoided; self-contained dist; provenance wired)
- Release-time automation publishes the package and moves the v0 major tag
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-publication/05-02-SUMMARY.md`
</output>
