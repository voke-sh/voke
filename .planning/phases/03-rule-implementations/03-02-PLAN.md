---
phase: 03-rule-implementations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/linter/src/rules/description.ts
  - packages/linter/src/rules/naming.ts
  - tests/rules/description.test.ts
  - tests/rules/naming.test.ts
  - tests/fixtures/rules/description-pass.json
  - tests/fixtures/rules/description-fail.json
  - tests/fixtures/rules/naming-pass.json
  - tests/fixtures/rules/naming-fail.json
  - tests/fixtures/rules/naming-duplicate-surface.json
autonomous: true
requirements: [RULE-02, RULE-03, RULE-06]
must_haves:
  truths:
    - "D01-D03 each fire on a known-bad tool and stay silent on a known-good tool"
    - "D02 and D03 fire INDEPENDENTLY on the 'search' worked example (no rule suppression) — both appear"
    - "N01-N02 are per-tool; N03 is the ONLY server-scoped rule (target:'server', tool:null) firing on duplicate names across the surface"
    - "Every finding carries ruleId, correct dimension, spec severity, location {tool, path[]}, message, verbatim fixHint"
    - "All description and naming rule functions perform zero IO (pass under fetch-stub)"
  artifacts:
    - path: "packages/linter/src/rules/description.ts"
      provides: "descriptionRules: RuleDefinition[] for D01-D03"
      contains: "export const descriptionRules"
    - path: "packages/linter/src/rules/naming.ts"
      provides: "namingRules: RuleDefinition[] for N01-N03 (N03 target:'server')"
      contains: "export const namingRules"
    - path: "tests/rules/description.test.ts"
      provides: "positive+negative coverage for D01-D03 incl. the search worked example; network-block stub"
      contains: "stubGlobal"
    - path: "tests/rules/naming.test.ts"
      provides: "positive+negative for N01-N02 + server-scoped N03 duplicate test; network-block stub"
      contains: "stubGlobal"
  key_links:
    - from: "packages/linter/src/rules/naming.ts"
      to: "packages/linter/src/engine/types.ts"
      via: "N03 RuleDefinition with target: 'server'"
      pattern: "target: 'server'"
    - from: "packages/linter/src/rules/naming.ts"
      to: "ctx.surface"
      via: "N03 reads ctx.surface (not ctx.tool) to detect duplicate names"
      pattern: "ctx\\.surface"
---

<objective>
Implement the 3 Description-as-Prompt rules (MTQS-D01..D03, dimension 'description', 1.2x) and the
3 Naming rules (MTQS-N01..N03, dimension 'naming', 1.0x), each dimension in its own module.

Purpose: D-rules cover agent usability (RULE-02); N-rules cover spec-compliance naming (RULE-03).
N03 is the single server-scoped rule in MTQS v0.1 and is the reference case for the engine's
server-aggregate routing (target:'server', ctx.tool === null, reads ctx.surface).
Output: `description.ts` (descriptionRules) + `naming.ts` (namingRules), with positive + negative
fixtures and network-blocked tests (RULE-06).

This plan owns ONLY its rule modules under `packages/linter/src/rules/` and `tests/`. It does NOT
touch `registry.ts` or `index.ts` — the Wave 2 integration plan (03-05) wires everything.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@spec/MTQS-v0.1.md
@spec/mtqs-v0.1.yaml
@packages/linter/src/engine/types.ts
@packages/linter/src/ingestion/types.ts
@packages/linter/src/engine/runner.ts

<interfaces>
<!-- Use directly — no exploration. Same Finding/RuleDefinition/RuleContext as 03-01. -->

From packages/linter/src/engine/types.ts:
```typescript
export type RuleTarget = 'tool' | 'server';
export interface Finding { ruleId: string; dimension: DimensionId; severity: Severity;
  message: string; location: { tool: string; path: string[] }; fixHint: string; }
export interface RuleContext {
  readonly tool: ToolSnapshot | null;            // null for server-scoped rules (N03)
  readonly surface: ReadonlyArray<ToolSnapshot>; // full sorted surface — N03 reads this
  readonly config: Readonly<VokeConfig>;
}
export type RuleFunction = (ctx: RuleContext) => Finding[];
export interface RuleDefinition { id; description; dimension; target; defaultSeverity; fixHint; mtqsVersion; fn; }
```

From packages/linter/src/engine/runner.ts (server-scoped routing — DO NOT modify, just rely on it):
```text
For target:'server' rules, the runner calls fn ONCE with ctx.tool === null and the full
sorted ctx.surface. For server findings, set location.tool to '' (engine convention:
FindingLocation.tool is '' for server-scoped findings).
```

From packages/linter/src/ingestion/types.ts:
```typescript
export interface ToolSnapshot { toolId: string; contentHash: string; name: string;
  title?: string; description?: string; inputSchema: Record<string, unknown>;
  outputSchema?: Record<string, unknown>; annotations?: Record<string, unknown>; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement descriptionRules D01-D03 (incl. the independent-firing search worked example)</name>
  <files>packages/linter/src/rules/description.ts, tests/rules/description.test.ts, tests/fixtures/rules/description-pass.json, tests/fixtures/rules/description-fail.json</files>
  <read_first>
    - spec/MTQS-v0.1.md sections MTQS-D01, MTQS-D02, MTQS-D03 (exact check, severity, message, fix hint, examples)
    - spec/MTQS-v0.1.md §4.4 "Worked Example: search Tool" (D02 AND D03 BOTH fire — no suppression)
    - spec/mtqs-v0.1.yaml entries MTQS-D01..D03 (copy fixHint VERBATIM)
    - STATE.md decision: "D02 and D03 fire independently in MTQS — no rule-suppression logic"
    - packages/linter/src/engine/types.ts and ingestion/types.ts
  </read_first>
  <behavior>
    All `target: 'tool'`, `dimension: 'description'`, `mtqsVersion: '0.1'`, operate on `ctx.tool`.
    - D01 (error): fires when `tool.description` is absent, null, or empty string (after no trimming — empty string '' fires; the spec failing example is ""). location.path = ['description']. Returns [] when non-empty.
    - D02 (warning): fires when `tool.description` is present and its `.length < 20`. location.path = ['description']. message interpolates the actual character count. Returns [] when length >= 20. IMPORTANT: D02 fires INDEPENDENTLY of D03 — do not suppress D02 when description equals name.
    - D03 (error): fires when `tool.description` is a byte-for-byte equal to `tool.name` (strict ===). location.path = ['description']. Returns [] otherwise. Fires INDEPENDENTLY of D02.
    The 'search' tool (name:'search', description:'search') MUST produce BOTH a D02 finding (length 6 < 20) AND a D03 finding (description === name). D01 does NOT fire (description is non-empty).
    Tests (RED first): the search worked example yields exactly [D02, D03] from descriptionRules; description-pass.json yields none.
  </behavior>
  <action>
    Create `packages/linter/src/rules/description.ts` exporting `descriptionRules: RuleDefinition[]`
    with D01, D02, D03. Severities VERBATIM from yaml: D01 error, D02 warning, D03 error. fixHints VERBATIM.
    Messages following spec §3:
      - D01: `MTQS-D01 [error] description is absent or empty: agents cannot determine what this tool does`
      - D02: `MTQS-D02 [warning] description is <n> characters: minimum is 20 characters for a meaningful description`
      - D03: `MTQS-D03 [error] description is a byte-for-byte copy of the tool name "<name>": adds no information`
    Each finding: { ruleId, dimension:'description', severity:<defaultSeverity>, message, location:{tool:ctx.tool!.toolId, path:['description']}, fixHint }.
    Pure functions of ctx.tool only. No IO, no Date.now, no Math.random, no fetch. No `any` type.
    Create `tests/fixtures/rules/description-pass.json` (substantive description >= 20 chars, not equal to name → zero D findings) and `tests/fixtures/rules/description-fail.json` containing at least: empty-description tool (D01), short-description tool (D02 only), and the canonical `search` tool {name:'search', description:'search'} (D02 + D03).
    Create `tests/rules/description.test.ts`: import descriptionRules, run each fn via a frozen RuleContext, assert per-rule positive fires + negative silent, AND a dedicated assertion that the `search` fixture yields findings whose ruleIds, sorted, equal ['MTQS-D02','MTQS-D03']. Add network-block via `vi.stubGlobal('fetch', () => Promise.reject(new Error('Network blocked in tests')))` in beforeEach and assert no rule throws.
  </action>
  <verify>
    <automated>npx vitest run tests/rules/description.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/linter/src/rules/description.ts` contains `export const descriptionRules` and substrings `'MTQS-D01'`, `'MTQS-D02'`, `'MTQS-D03'`.
    - `descriptionRules` has length 3 (test asserts `toHaveLength(3)`).
    - Test asserts the `search` fixture produces ruleIds `['MTQS-D02','MTQS-D03']` (both fire, no suppression).
    - Test cross-checks each descriptionRules entry's defaultSeverity + fixHint against spec/mtqs-v0.1.yaml (loaded via @voke/core loadRegistryFile, trimmed compare).
    - tests/rules/description.test.ts contains `stubGlobal`.
    - `grep -E "fetch|Date\.now|Math\.random|: any" packages/linter/src/rules/description.ts` returns nothing.
    - `npx vitest run tests/rules/description.test.ts` exits 0; `npx tsc --noEmit -p packages/linter` exits 0.
  </acceptance_criteria>
  <done>D01-D03 implemented as pure RuleDefinitions; the search worked example fires D02+D03 independently; fixtures + network-block test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement namingRules N01-N02 (per-tool) + N03 (server-scoped duplicate-name)</name>
  <files>packages/linter/src/rules/naming.ts, tests/rules/naming.test.ts, tests/fixtures/rules/naming-pass.json, tests/fixtures/rules/naming-fail.json, tests/fixtures/rules/naming-duplicate-surface.json</files>
  <read_first>
    - spec/MTQS-v0.1.md sections MTQS-N01, MTQS-N02, MTQS-N03 (exact check, severity, message, fix hint, examples)
    - spec/mtqs-v0.1.yaml entries MTQS-N01..N03 (N03 scope: server; copy fixHints VERBATIM)
    - STATE.md decision: "MTQS-N03 is the only server-scoped rule"
    - packages/linter/src/engine/runner.ts (how target:'server' rules receive ctx.tool=null + ctx.surface)
    - tests/engine/runner.test.ts (the makeTool helper + server-scoped routing test pattern to mirror)
  </read_first>
  <behavior>
    N01, N02 are `target: 'tool'`, `dimension: 'naming'`. N03 is `target: 'server'`, `dimension: 'naming'`.
    - N01 (error): fires when `tool.name` is absent/empty OR `tool.name.length > 128`. location.path = ['name']. message interpolates length. Returns [] for 1..128 chars.
    - N02 (error): fires when `tool.name` contains any character outside `[A-Za-z0-9_\-./]`. Use regex `/[^A-Za-z0-9_\-./]/`. location.path = ['name']. message names the tool. Returns [] when all chars allowed.
    - N03 (error, server-scoped): reads `ctx.surface` (NOT ctx.tool, which is null). Group tools by `name`; for each name shared by 2+ tools, emit ONE finding. location.tool = '' (server convention), location.path = ['tools'] or ['name']. message: count of tools sharing the name. Emit findings deterministically (iterate names in sorted order; runner re-sorts findings anyway). Returns [] when all names unique.
    Tests (RED first): N01 fires on a 141-char name; N02 fires on "search contacts, all"; N03 fires once on a surface with two tools named 'search' and stays silent on a unique-name surface.
  </behavior>
  <action>
    Create `packages/linter/src/rules/naming.ts` exporting `namingRules: RuleDefinition[]` = [N01, N02, N03].
    Severities VERBATIM from yaml: N01 error, N02 error, N03 error. target: N01/N02 'tool', N03 'server'. fixHints VERBATIM.
    Messages following spec §3:
      - N01: `MTQS-N01 [error] tool name is <n> characters: maximum is 128 per MCP spec (SEP-986 recommends 1-64)` (and an absent/empty variant message for the missing-name case)
      - N02: `MTQS-N02 [error] tool name "<name>" contains illegal characters: allowed: [A-Za-z0-9_\-./]`
      - N03: `MTQS-N03 [error] tool name "<name>" is duplicated: <count> tools share this name, causing non-deterministic dispatch`
    N03 fn signature: read `ctx.surface`; build a Map<string, number> of name→count; for names with count>=2 push one finding with location:{tool:'', path:['name']}.
    Pure functions only. No IO, no Date.now, no Math.random, no fetch. No `any`.
    Create fixtures:
      - `tests/fixtures/rules/naming-pass.json` (a single tool with a clean 1-64 char name, allowed chars → zero N01/N02 findings)
      - `tests/fixtures/rules/naming-fail.json` (tools: one with a 141-char name for N01; one named "search contacts, all" for N02)
      - `tests/fixtures/rules/naming-duplicate-surface.json` (a surface ARRAY of ToolSnapshots with two tools both named "search" plus one unique tool — feeds N03)
    Create `tests/rules/naming.test.ts`:
      - per-tool tests for N01, N02 via frozen RuleContext ({tool, surface:[tool], config:{}})
      - N03 test: build a RuleContext with `tool: null` and `surface` = the duplicate-surface fixture; assert exactly ONE N03 finding with location.tool === '' and message containing "search"; then a unique-name surface yields zero N03 findings
      - network-block stub in beforeEach; assert no rule throws
  </action>
  <verify>
    <automated>npx vitest run tests/rules/naming.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/linter/src/rules/naming.ts` contains `export const namingRules`, substrings `'MTQS-N01'`, `'MTQS-N02'`, `'MTQS-N03'`, and the exact substring `target: 'server'` (for N03).
    - naming.ts references `ctx.surface` (N03 reads the surface, not ctx.tool).
    - `namingRules` has length 3; exactly one entry has `target === 'server'` (test asserts `namingRules.filter(r => r.target === 'server')` has length 1 and that entry's id is 'MTQS-N03').
    - N03 test asserts a single finding with `location.tool === ''` on the duplicate surface and zero findings on the unique surface.
    - Test cross-checks each namingRules entry's defaultSeverity + fixHint against spec/mtqs-v0.1.yaml.
    - tests/rules/naming.test.ts contains `stubGlobal`.
    - `grep -E "fetch|Date\.now|Math\.random|: any" packages/linter/src/rules/naming.ts` returns nothing.
    - `npx vitest run tests/rules/naming.test.ts` exits 0; `npx tsc --noEmit -p packages/linter` exits 0.
  </acceptance_criteria>
  <done>N01-N02 per-tool and N03 server-scoped implemented; N03 detects duplicate names from ctx.surface and emits a single server finding; fixtures + network-block test green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/rules/description.test.ts tests/rules/naming.test.ts` exits 0.
- descriptionRules length 3 (all dimension:'description', target:'tool'); namingRules length 3 (N01/N02 target:'tool', N03 target:'server').
- The 'search' worked example fires BOTH D02 and D03 (independent, no suppression).
- N03 reads ctx.surface and emits one finding per duplicated name with location.tool === ''.
- No IO in either module: `grep -E "fetch|Date\.now|Math\.random" packages/linter/src/rules/description.ts packages/linter/src/rules/naming.ts` returns nothing.
</verification>

<success_criteria>
RULE-02 and RULE-03 satisfied: D01-D03 and N01-N03 implemented with verbatim spec
severities/fixHints, positive + negative fixtures, the independent D02/D03 firing proven on the
search example, and N03 correctly server-scoped — feeding into RULE-06.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rule-implementations/03-02-SUMMARY.md`
</output>
